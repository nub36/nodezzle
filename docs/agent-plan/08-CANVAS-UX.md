# 08 — Удобство редактора

Владелец выбрал направление «Удобство редактора» после завершения плана 07.
Продолжаем существующий Canvas; серверное сохранение и продакшн-деплой
в этот этап не входят. План 07 не переоткрывается.

## A. Копирование и дублирование — завершён (0.5.35)

- [x] Один общий путь клонирования фрагмента: новые ID узлов/внутренних
  связей, независимые настройки; внешние связи не копируются.
- [x] Поиск свободного места для прямоугольника всего фрагмента на основе
  геометрии 07C, без изменения взаимных смещений и старых позиций.
- [x] Ctrl/Cmd+D, Ctrl/Cmd+C/V и контекстное меню используют актуальные
  границы между панелями; переполнение показывает новый фрагмент камерой.
- [x] Повторная вставка, undo/redo, F5, фактическая работа связей — в E2E;
  чистая геометрия и стор — в unit/integration.
- [x] Формат проекта v1, прогресс и существующие функции сохранены.

Фактически проверено: **454/454 unit/integration (59 файлов), 35/35 E2E**,
typecheck, строгие типы E2E, build. Новые проверки — 11 стора, 2 геометрии,
4 браузерных сценария. Первые три E2E до исправления воспроизводили наложения;
после исправления пройдены вместе с регрессией всего курса 12/12.
Геометрия — `src/lib/node-placement.ts`, решение — ADR-016.

## Поправки по скриншоту владельца — завершены (0.5.36), перед B

2026-09-14: владелец сообщил о зависании «Первой цепочки» на соединении,
перекрытии интерфейса и отсутствии понятного результата «Проверить шаг».
Запрос: перемещаемое окно урока, проверка всех 12 уроков на этот сбой,
затем возврат к плану. 08B пока не начат.

- [x] Перемещение окна указателем и клавиатурой, сохранение положения при
  переходах/F5, ограничение экраном и возврат к исходной позиции.
- [x] Карточка не перехватывает завершение соединения над портом.
- [x] Ручная проверка показывает, чего не хватает; неверный порт не
  засчитывается. Быстрая вставка закрывается без прерывания урока.
- [x] Регрессия всех 12 уроков, дополнительные сценарии по скриншоту.

Проверено: **471/471 unit/integration, 49/49 E2E**, типы и сборка.
Все 12 полных уроков повторно пройдены; 14 дополнительных браузерных
сценариев окна и 17 unit/integration. Развёртывание сайта не выполнялось.

## B. Панели на узких экранах — следующий подэтап

Проверить реальную доступную ширину Canvas. Спроектировать сворачивание
библиотеки/инспектора так, чтобы они не перекрывали весь холст; проверить
клавиатуру и работу уроков. Не выдавать текущие тесты Академии на 390 px
за полную мобильную адаптацию редактора.

## Границы

В A не вводятся глобальная раскладка, перенос определений моделей между
проектами или копирование визуальных рамок групп. Внутренний буфер деталей
не заменяет системный буфер текста; сочетания в полях ввода не перехватываем.
После каждого стабильного подэтапа: typecheck, test, build, E2E, документация,
русский commit и push только в рабочую ветку сессии.
