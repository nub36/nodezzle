# STATUS — где остановилась разработка

**Обновлено: 2026-09-13.** Этот файл — единственная «точка остановки» проекта.
Обновляй его после каждого стабильного подэтапа (см. [05-DEVELOPMENT-DEPLOY.md](05-DEVELOPMENT-DEPLOY.md)).
Все значения ниже взяты **по фактическому Git на 2026-09-13**, ничего не придумано.

| Поле | Значение |
|---|---|
| Текущая ветка | `arena/01a09bee-nodezzle` (содержит фундамент `a1b0c20` + зафиксированный план; ветка `main` пока только `f0bec2f` — см. «Известные проблемы») |
| Последний стабильный коммит (код) | `a1b0c20` — «Этап 0: технический фундамент NODEZZLE (ядро, runtime, Canvas, 24 блока, формат v1, ru-RU UI, документация, тесты)» |
| Последний стабильный коммит (план) | коммит сразу после `a1b0c20` с сообщением `docs: добавить постоянный поэтапный план разработки NODEZZLE` — точный хеш: `git log -1` на текущей ветке |
| Текущий промпт / этап | **Этап 2 — Canvas UI (Промпт №2)**, документ [02-CANVAS-UI.md](02-CANVAS-UI.md) |
| Текущий подэтап | не начат (впереди подэтапы A–I) |

## Выполнено

- **Этап 1 — Фундамент (Промпт №1)**: полностью выполнен в коммите `a1b0c20`
  (подробности — [01-FOUNDATION.md](01-FOUNDATION.md)): ядро, реестр блоков,
  система типов, runtime, формат проекта v1, сериализация, автосохранение,
  undo/redo, Canvas, симулятор Telegram, отладка, дашборд, лендинг,
  локаль `ru-RU`, 34 unit-теста, 12 документов. **Не переделывать.**
- **Организационная задача 2026-09-13**: создан постоянный план
  `docs/agent-plan/` (этот каталог), обновлён корневой `AGENTS.md`.

## В процессе

- Ничего (после фиксации плана разработка не велась).

## Следующее

1. **Этап 2 — Canvas UI**, начать с подэтапа **A: UI Store + Библиотека v2**
   ([02-CANVAS-UI.md](02-CANVAS-UI.md)); далее B, C, … по порядку или по решению владельца.
2. Разобрать «Известные проблемы» (в первую очередь — вопрос слияния в `main`).

## Известные проблемы

1. **`main` не содержит фундамент и план.** Фундамент `a1b0c20` существует в
   ветках `arena/01a09a3f-nodezzle`, `arena/01a09b67-nodezzle` и
   `arena/01a09bee-nodezzle` (эта ветка — самая полная: фундамент + план).
   В `main` пока только `f0bec2f` (README). Нужно влить актуальную ветку в
   `main` (через пул-реквест), чтобы `main` стал единой точкой правды.
2. **Телеграм — только симуляция**: реального транспорта бота нет
   (это этап 2 дорожной карты `docs/ROADMAP.md`).
3. **Хранилище — только LocalStorage**: серверного хранилища и базы данных нет.
4. **Шесть зарезервированных блоков** без реализации (`available: false` в
   `src/blocks/reserved.ts`) — скрыты из палитры, это ожидаемо.
5. **Нет продакшн-развёртывания и нет серверной части** (см. [05-DEVELOPMENT-DEPLOY.md](05-DEVELOPMENT-DEPLOY.md)).
6. Сборка выдаёт предупреждение о размере фрагмента больше 500 килобайт —
   не ошибка, учесть при развитии интерфейса.

## Результаты проверок (фактические, 2026-09-13)

| Проверка | Результат |
|---|---|
| Последний результат тестов | **34/34 пройдено** (5 файлов, `npm test`, Node 22) |
| Последний результат сборки | **Сборка успешна** (`npm run build`; проверка типов `npm run typecheck` — без ошибок) |

## Развёртывание

| Поле | Значение |
|---|---|
| Последний развёрнутый коммит | отсутствует — продукт ещё не развёрнут |
| Продакшн-адрес | `https://nodezzle.duckdns.org` — **запланирован**, ещё не развёрнут (правила деплоя — [05-DEVELOPMENT-DEPLOY.md](05-DEVELOPMENT-DEPLOY.md)) |

## Как обновлять этот файл

После каждого стабильного подэтапа обнови: ветку, последний коммит,
этап/подэтап, списки «Выполнено / В процессе / Следующее», результаты тестов и
сборки, состояние развёртывания. Значения бери **из фактического Git и фактических
запусков команд**, а не из памяти.
