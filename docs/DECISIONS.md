# NODEZZLE — Архитектурные решения (ADR)

Обоснованные технические решения проекта. Каждое решение: контекст → решение → альтернативы → последствия.

---

## ADR-001 — Стек фронтенда: Vite + React 19 + TypeScript

**Контекст.** NODEZZLE — heavyweight-SPA (Canvas с тысячам узлов, live-выполнение, i18n). Нужны: быстрая разработка, строгая типизация, зрелая экосистема для node-graph UI, лёгкий dev/build.

**Решение.** **Vite** (dev/build, HMR, alias `@` → `src/`) + **React 19** + **TypeScript (strict)** + `moduleResolution: bundler`.

**Альтернативы.**
- *Next.js* — SSR/SSG избыточны: продукт — десктоп-SPA; усложняет превью/статическую раздачу. Отклонено.
- *Svelte/Solid* — меньше экосистемы именно для graph-редакторов. Отклонено.
- *Без TS* — формат проекта, контракты блоков и runtime требуют строгих типов. Отклонено.

**Последствия.** `tsc --noEmit` в CI-процедуре (npm run build/typecheck); TS-типы выведены из Zod-схемы формата.

## ADR-002 — Canvas: @xyflow/react (React Flow 12)

**Контекст.** Нужен бесконечный Canvas: drag&drop, зум/пан, соединения, миникарта, производительность на больших схемах, кастомные узлы, API для интеграции (screenToFlowPosition, applyNodeChanges…).

**Решение.** **@xyflow/react 12** — зрелая библиотека node-graph: встроено всё необходимое, кастомные узлы/рёбра, controls, minimap, виртуализация рендера.

**Альтернативы.**
- *Собственный Canvas (SVG/Canvas2D/WebGL)* — месяцы работы, риск деградации качества; переписывание при росте. Отклонено.
- *jointjs / litegraph.js / react-flow-diagram* — слабее экосистема, устаревание, меньше контроля типов. Отклонено.
- *LogicFlow / X6* — API и экосистема слабее для React-стека. Отклонено.

**Последствия.** Кастомный узел `NodezzleNode` рендерится из Block Definition; UI-состояние хранилища отделено от формата (serialize.ts).

## ADR-003 — Состояние: Zustand

**Контекст.** Состояние: проект/canvas (nodes/edges), undo/redo, autosave, execution (статусы узлов, логи, outbox). Нужно: предсказуемость, минимум бойлерплейта, селекторы (узлы перерисовываются по своим слайсам), доступ вне React (runtime-колбэки).

**Решение.** **Zustand 5**: два стора — `project-store` (документ, undo/redo, autosave) и `execution-store` (выполнение).

**Альтернативы.**
- *Redux Toolkit* — больше ритуала, не критично для SPA такого масштаба. Отклонено.
- *Context+reducer* — перерисовки всего дерева, неудобные селекторы. Отклонено.
- *Jotai/Recoil* — атомарность избыточна. Отклонено.

**Последствия.** Undo/redo — явная история снапшотов (100), не «магия» middleware; autosave — debounce 900 мс + flush on beforeunload.

## ADR-004 — i18n: i18next + react-i18next, основная локаль ru-RU

**Контекст.** Весь UI и документация — на русском; технические id — английские; архитектура должна позволить добавить языки позже.

**Решение.** **i18next** с одним словарём `ru.json` (неймспейсы внутри: app/common/landing/dashboard/canvas/execution/errors/portTypes/categories/blocks). Все пользовательские строки — ключи; новые блоки несут свои `labelKey/descriptionKey`.

**Альтернативы.**
- *Harдкод строк* — нарушение требований продукта. Отклонено.
- *messageformat/полиморфные словари* — избыточно на MVP. Отклонено.

**Последствия.** Правила: русские тексты только в `ru.json`; `errors.*` — код=англ., текст=рус.

## ADR-005 — Tailwind CSS 4 (утилиты) + собственные токены/декор

**Контекст.** Premium Dark UI (glassmorphism, aurora, glow) требует: дизайн-токены, быстрые утилиты, кастомные декоративные классы без тяжёлых библиотек анимации.

**Решение.** **Tailwind CSS 4** (через `@tailwindcss/vite`): токены в `@theme` (abyss/navy/panel/line/ink/muted), декоративные классы (aurora, glass, noise, hero-*, play-*, status-*) — вручную в `globals.css` (чистый CSS + SMIL, **без framer-motion** на этом этапе).

**Альтернативы.**
- *styled-components/CSS Modules* — дублирование утилит, медленнее итерации. Отклонено.
- *framer-motion* — лишняя зависимость; анимации покрывает CSS/SMIL. Пересмотреть при появлении сложных переходов. Отложено.

**Последствия.** Один CSS-файл стилей; токены — единая точка правды цвета.

## ADR-006 — Формат проекта: Zod (schema-first)

**Контекст.** Формат проекта — контракты между UI, runtime и будущим backend. Нужны: валидация при загрузке, вывод типов, версионирование.

**Решение.** **Zod 4**: `nodezzleProjectSchema` — единственный источник правды (`z.infer` → TS). Проверка: `tryParseProject` при чтении из хранилища; runtime дополнительно валидирует документ (существование блоков/узлов соединений).

**Альтернативы.**
- *TypeBox/EJSON* — меньше интеграции с TS-выводом и экосистемой. Отклонено.
- *Чистые TS-интерфейсы без валидации* — мусорные данные ломают runtime. Отклонено.

**Последствия.** Блоки/категории не перечислены в схеме формата (ссылка `blockId` строкой) — формат не меняется при добавлении блоков.

## ADR-007 — Хранилище: интерфейс ProjectStorage + LocalStorageAdapter

**Контекст.** MVP без backend; впереди — серверное хранилище, версии, пользователи.

**Решение.** Интерфейс `ProjectStorage` (list/get/save/remove) + `LocalStorageAdapter` (ключ `nodezzle.project.<id>`). Flush на `beforeunload`. Замена на `ApiStorageAdapter` — без изменения UI (ROADMAP этап 2–3).

**Альтернативы.** *IndexedDB* — асинхронная сложность без пользы для MVP (размеры проектов малы). Отклонено до этапа «большие проекты». *SQLit/WASM* — избыточно. Отклонено.

## ADR-008 — Runtime: чистый TypeScript, `executeCanvas`

**Контекст.** Выполнение схем: данные по соединениям, триггеры, модели, ошибки, отмена, циклы. Должно тестироваться без DOM и в будущем работать на сервере (Telegram webhook → execution).

**Решение.** `executeCanvas(doc, options) → ExecutionResult` — чистая асинхронная функция без React: очередь готовности (push), pull «холодных» источников (on-demand исполнение констант), бюджет шагов (циклы), cancel-флаг, телеметрия (nodeRuns/logs/outbox). Telegram-транспорт — инъекция `runtime.telegram` (MVP: outbox-симуляция).

**Альтернативы.**
- *Выполнение внутри React-стека (стор как движок)* — не тестируемо, не переиспользуемо на сервере. Отклонено.
- *WASM/worker на этапе 0* — преждевременно (см. ROADMAP этап 7). Отложено.

**Последствия.** 10+ unit-тестов runtime; тот же код ляжет в будущий backend.

## ADR-009 — Умные соединения: правила в ядре, UI — подсветка

**Контекст.** Совместимость INPUT/OUTPUT должна быть единой для UI (подсветка/запрет) и для будущего (валидации, AI-проверки).

**Решение.** `checkCompatibility(source, target)` в `src/core/type-system` (чистая функция, тестируется): error↔error, event↔event, data: `any` со всем / равенство типов / TypeRegistry для кастомных. Canvas: `isValidConnection` запрещает несовместимые; во время drag — подсветка портов.

**Последствия.** Новые типы/правил добавляются в одно место.

## ADR-010 — Роутинг: react-router-dom (HashRouter)

**Контекст.** Три экрана (/, /dashboard, /projects/:id). Статическая раздача превью/сборки без серверных rewrite.

**Решение.** **react-router-dom 7** + **HashRouter** (`/#/dashboard`): работает из любого статического хостинга/превью без настройки. При появлении backend-хостинга — переход на BrowserRouter без изменения кода экранов.

## ADR-011 — Тесты: Vitest (node, без jsdom)

**Контекст.** Ядро (типы, реестр, формат, storage, runtime) — чистый TS. UI-тесты не требуются на этапе 0 (ручная проверка превью).

**Решение.** **Vitest** (node-среда): 34 unit-теста; `*.test.ts` рядом с кодом; storage — in-memory fake (без mock-библиотек).

## ADR-012 — Безопасность секретов

**Контекст.** Публичный репозиторий; продукт будет использовать Telegram Bot Token и API-ключи.

**Решение.**
1. В Git — только `.env.example` (названия + фиктивные значения, помеченные `EXAMPLE-FIXTURE-DO-NOT-USE`).
2. `.env`/`.env.*` в `.gitignore` (исключение `.env.example`).
3. Telegram Bot Token — **только на backend** (в будущем); frontend-адаптер — симуляция (outbox).
4. Тип порта `secret` существует; значения секретов в формате проекта не хранятся (в будущем — ссылки на серверные секреты).
5. Перед каждым commit — проверка отсутствия секретов (AGENTS.md §6).

## ADR-013 — MVP-направления: только Telegram и Web

**Контекст.** Заявка продукта: два направления; другие платформы — при расширении.

**Решение.** Реализуются категории `telegram` и `web` (+ общие: core/logic/data/flow/models/debug/datetime). Категории `http`, `ai`, `memory` зарезервированы (блоки `available: false` — документация архитектуры без ложных обещаний UI).

**Последствия.** Архитектура проверена «на совместимость с будущим»: новый канал = модуль блоков + регистрация (проверено двумя платформами).

## ADR-014 — Именование: `nodezzle`, branch-политика

**Контекст.** Долговременная память проекта — один репозиторий `nub36/nodezzle`.

**Решение.** Все изменения — в этом репозитории; коммиты с русским описанием; документация синхронизируется (AGENTS.md §5). PR-процесс: ветка → PR в `main` (по решению владельца).

---

*Новые ADR добавляются в конец; отменённые решения помечаются «ОТМЕНЕНО → ссылка на новое».*


## ADR-015 — Свободное место при вставке, без перераскладки схемы (0.5.34)

**Контекст.** Компактные детали всё равно накладывались при повторном клике
в библиотеке. Существующие вручную заданные позиции должны сохраняться.

**Решение.** Геометрия — чистая функция UI-слоя; Canvas передаёт актуальные
размеры, препятствия и видимую область в координатах холста. Поиск по
полосам/интервалам выбирает целую свободную позицию с зазором 24.
При заполнении viewport используется свободная точка справа от схемы,
камера показывает её. `project-store.addNode` и сериализатор не меняются.

**Последствия.** Единое поведение для клика, drop, стартовой вставки и
меню от порта; без новых зависимостей и формата проекта. Измерения и
viewport остаются временными UI-данными. Старые позиции не изменяются;
не выполняются глобальная раскладка, устранение старых наложений или
переразмещение при позднем росте узлов. Буфер/дублирование вне этого изменения.


## ADR-016 — Фрагмент как единица размещения (0.5.35, 08A)

**Контекст.** Владелец выбрал Canvas UX после плана 07. Смещение дубликатов
на +36/+36 и случайная точка paste создавали наложения; поузловой поиск
разрушил бы взаимную раскладку копии.

**Решение.** Геометрия ADR-015 переносится из `features/canvas` в `lib`,
чтобы стор не зависел от feature-слоя. Добавлены общие границы фрагмента.
Приватный `insertFragment` в сторе находит свободное место для всего
прямоугольника, переносит узлы одним смещением, копирует лишь внутренние
связи и создаёт один снимок истории. Буфер глубоко копирует узлы и рёбра.
UI передаёт доступную область, получает только новые ID и показывает
фрагмент существующей камерой; mutable-содержимое буфера не раскрывается.

**Последствия.** Внутренняя раскладка, порты, старые позиции и формат v1
сохранены; зазор 24, без глобальной раскладки и новых зависимостей.
Пустоты внутри границ не используются, старые наложения не исправляются.
Сериализатор продолжает округлять дробные координаты: точное взаимное
смещение гарантируется в операции вставки, а не после округления v1.
Рамки групп и определения моделей не дублируются; ограничения минимального
масштаба и узких экранов остаются явными (UI.md, план 08B).


## ADR-017 — Перемещаемая подсказка без блокировки соединений (0.5.36)

**Контекст.** Владелец показал зависание «Первой цепочки»: окно поверх
редактора, неверный выход user_id вместо text, быстрая вставка. Отсутствие
обратной связи делало корректный отказ зачёта похожим на поломку.

**Решение.** Не ослаблять completion. Окно перемещается через Pointer
Capture и стрелки за отдельный заголовок; координаты только в sessionStorage,
ограничение экраном — чистая функция и ResizeObserver. Во время соединения
pointer-events карточки выключены. Ручная проверка сообщает оставшееся
условие, connect — конкретные порты. Escape меню обрабатывается раньше
глобального обработчика урока. Затемнение снижено до 35%.

**Последствия.** Схемы, прогресс и правила уроков не мигрируются. Начать
жест из-под окна нельзя — его нужно переместить/свернуть, но завершить
провод над закрытым портом теперь можно. Серверный сайт не развёртывается
автоматически этим изменением; 08B остаётся следующим подэтапом.


## ADR-018 — Одна панель на узком Canvas (0.5.37, 08B)

**Контекст.** Две постоянные панели по 280 px перекрывали узкий холст.
Скрывать функции или менять позиции схемы при resize нельзя.

**Решение.** Ниже 1024 px одна немодальная панель либо свободный холст;
временный `canvasPanel` в UI-store, DOM `hidden`, явные переключатели,
фокус/возврат и приоритет Escape. Вставка закрывает панель и использует
весь будущий холст. Дополнительные инструменты тулбара раскрываются ниже
1280 px, отладка прокручивается и ограничена по высоте. Учебная подсветка
показывает переключатель скрытой панели, не открывает её за пользователя.

**Последствия.** Нет миграции проекта/прогресса, новых зависимостей или
автоперераскладки. Настольный режим сохраняется. Порог одинаков в hook и
CSS. Это адаптация панелей; сенсорные жесты и полный мобильный курс не
объявляются проверенными на основании трёх E2E.


## ADR-019 — Адрес события отдельно от его данных (0.5.38, 09A)

**Контекст.** После выбора направления Telegram/Web выявлено, что превью
фильтрует только тип события: несколько кнопок/форм запускаются вместе.
Подписи не уникальны, перенос адреса в конфигурацию потребовал бы правки
схем и ломал бы дублирование.

**Решение.** Необязательный `targetNodeId` в конверте `TriggerPayload`.
UI передаёт существующий ID экземпляра, runtime фильтрует входы и затем
проверяет matches. Нет адреса — старое поведение. Неверный адрес — ожидание,
не автоматический запуск всех. Выходные web-данные не меняются. Для формы
отдельный чистый парсер принимает JSON-объект, остальные данные не отправляются.

**Последствия.** Нет миграции проекта, новых блоков или зависимостей.
Адрес локален активному холсту, не заменяет проверку доступа серверного API.
Вызовы моделей не наследуют внешний адрес. Первая страница остаётся
единственной отображаемой; полный рендерер — последующий подэтап 09B.


## ADR-020 — Инертные Web-листья и актуальность результата (2026-09-14)

**Контекст.** Полная структура страницы ещё не определена. Нельзя выдавать
все десять UI-блоков за готовые или вставлять пользовательский HTML. После
правки графа старые nodeInfo также нельзя показывать как текущую страницу.

**Решение.** В 09B1 реализовать только web.text/web.heading: типизированные
инертные дескрипторы, p/h1–h6 с React-текстом, плоский порядок. Входы с
данными приоритетнее настроек; недопустимые значения — ERR_WEB_ELEMENT.
Статические элементы строятся из config сразу, проводные — только из
успешного результата узла и совпадающего снимка данных выполнения.
Ключ захватывается до async-запуска, содержит контекст проекта/модели,
данные узлов/связей и определения моделей, но не геометрию активного холста.
Изменения определений моделей целиком инвалидируют ключ консервативно.

**Последствия.** Не требуется DOM в runtime, новая зависимость или миграция.
Публикация сайта/контейнеры — следующие подэтапы; существующие правила
триггерных цепочек сохраняются. Результат в превью после F5 не берётся из
локальной истории. При большом графе ключ сериализуется целиком; инкрементная
версионизация возможна позднее, но не должна терять семантические изменения.


## ADR-021 — Массивы дескрипторов и корни Web-структур (2026-09-14)

**Контекст.** У трёх planned-структур уже есть children:array и element:object.
Нельзя молча разрешить object → array, выполнять произвольный HTML или
дублировать исходный лист рядом с собранным родителем.

**Решение.** Сохранить порты и собирать children существующими core.array и
data.array_add. Реализовать только container/section/grid поверх листьев
09B1. Валидатор создаёт инертную копию: виды из белого списка, текстовые
дети, колонки 1–6, бюджеты 16 уровней/256 узлов/65536 символов на дерево.
Циклические/невалидные значения целиком отклоняются с ERR_WEB_TREE.
Порядок массива задаёт порядок детей. Структуры могут вкладываться друг в друга.

Корни выбираются по графу: источник UI в data-зависимостях children структуры
скрывается снаружи. Это консервативно, не идентификация JSON и не анализ
фактической ветки; удаление элемента промежуточным блоком не восстанавливает
его корневой вывод. Общие дети разрешены; циклы графа отдельно диагностируются.
Подключённые корни используют защиту свежести 09B1, а статические пустые
структуры строятся из настроек. Кнопки/формы/page сохраняют слой 09A.

**Последствия.** Без новых типов портов, runtime-спецслучаев, зависимостей,
миграции или скрытого HTML. Собранные структуры доступны в основном и
внутреннем холстах, но не раскрывают автоматически внутренние узлы модели
снаружи. Массивы требуют нескольких блоков; это объяснено пошаговой практикой
в WEB.md. Автоматический UI-компоновщик, полный Web-рендерер и публикация
остаются последующими задачами.


## ADR-022 — HTTPS URL и явная внешняя загрузка (2026-09-15)

**Контекст.** Image/link должны работать и отдельно, и внутри дерева B2.
Проект может прийти из недоверенного источника; автоматические img-запросы
раскрывали бы сетевые данные при одном открытии превью. Относительные URL
не имеют однозначной базы: адрес рамки не является опубликованным сайтом.

**Решение.** Общий чистый валидатор абсолютного HTTPS: DNS-host, порт 443,
без credentials/IP/типичных локальных имён/управляющих символов; URL до 2048.
Дескрипторы содержат только URL и текст. Входы приоритетнее настроек.
Подписи до 65536, в дереве URL включаются в общий бюджет строк B2.
Изображение — по явной кнопке, no-referrer, с ошибкой и повтором; смена URL
или контекста сбрасывает согласие. Ссылка — новая вкладка, noopener/noreferrer,
без доступа к редактору через opener. Runtime не выполняет запросы.

**Последствия.** Нет поддержки относительных адресов, http/data/blob/file,
загрузки файлов на сервер и asset-прокси. Валидатор не контролирует DNS
или редиректы; это не гарантия безопасности сайта и не SSRF-фильтр для
будущего серверного fetch. IP/cookies клиента внешнему источнику могут быть
видны после согласия. Без новой зависимости/формата, публикация не включена.


## ADR-023 — Явный режим формы и привязка полей по ID (2026-09-15)

**Контекст.** JSON-формы 09A уже работают. Поля могут находиться в дереве,
а одинаковые подписи не являются адресами. Автоматическое смешивание
ввода с JSON и молчаливая отправка неполных данных недопустимы.

**Решение.** formMode=json|fields с прежним json по умолчанию. Поля input/
textarea сохраняют порты label/placeholder, добавляют настройки имени,
ID формы и начального значения. Валидируемый инертный дескриптор переносит
эти данные в дерево. Нативные контролы связаны с отдельной формой через
HTML form-атрибут и уникальный префикс экземпляра превью.

Набор ожидаемых полей берётся из того же актуального дерева, что видит UI;
FormData проверяется повторно. Дубли/пустой набор/лимиты блокируют отправку,
а любой неактуальный/ошибочный корень консервативно блокирует режим fields
(не JSON), чтобы не терять скрытые поля. Ввод не создаёт события до submit.
Копирование явного field-блока переназначает config.formNodeId при наличии
формы во фрагменте, иначе очищает ссылку. Произвольный JSON не переписывается.

**Последствия.** Без новых портов, изменения runtime/формата v1 и скрытого
хранилища черновиков. Контекст/дескриптор/размонтирование сбрасывают ввод;
отправленные значения остаются обычными данными истории. Проводные поля
требуют выполнения своей цепочки по прежним правилам триггеров. Модальное
окно, обязательные/числовые/файловые поля и input_change остаются вне B4.


## ADR-024 — Локальное информационное окно без автоматического запуска (2026-09-15)

**Контекст.** web.modal имел контракт title/children → element, но не вывод.
Запуск runtime заменяет актуальные результаты. Если открытие напрямую
отправлять в modal_open, оно может тут же размонтировать собственное окно.
Поля B4 внутри нативного модального диалога связались бы с недоступной
фоновой формой либо отправлялись скрытыми; такое поведение не принимается.

**Решение.** Информационный дескриптор modal/title/children в общем валидаторе
B2. Рекурсивно запрещены поля и вложенные модальные окна; обычные структуры,
текст и URL разрешены. Ошибка отклоняет всё дерево. WebModalView использует
нативный dialog/showModal с ручным открытием и локальным состоянием. Escape,
кнопка закрытия, Tab-цикл, возврат фокуса, восстановление после исчезновения
активного контрола. Window capture изолирует клавиши раньше обработчиков
редактора/React Flow на document; обработчик и наблюдатель DOM очищаются.

Дети монтируются только при открытии, поэтому закрытие сбрасывает согласие
на картинки. Полный дескриптор/контекст и запуск сбрасывают состояние.
Нет автоматического fireWebTrigger: modal_open/close остаются явными
событиями симулятора/API, что написано непосредственно в UI и обучении.

**Последствия.** Минимальная информационная модальность, без стека окон,
скрытых форм и разрушения диалога побочным запуском. Автособытия требуют
отдельного решения жизненного цикла. Движок, транспорт, формат v1 и
зависимости не меняются; публикация сайта не входит в B5.


## ADR-025 — Callback как отдельное событие и действие outbox (2026-09-15)

**Контекст.** Серверный Bot API уже имеет answerCallbackQuery, но webhook
нормализовал только сообщения, а две callback-детали были planned.
Callback нельзя направлять в message_received/command или изображать
подтверждение как обычное сообщение чата. Нужна сквозная проверка без токена.

**Решение.** 09C разделён на C1 (callback/ответ) и C2 (клавиатура/кнопки).
C1 добавляет необязательный discriminator telegram.event; без него работает
прежний контракт сообщения. Callback обычного доступного сообщения проходит
общую проверку ID/UTF-8 data. Источник и event проверяются в matches блоков,
не в планировщике. Фильтр по данным точный, пустой — все callbacks.

К существующим callback_query/answer_callback добавлен текстовый порт
callback_id (старые порты сохранены). Ответ использует явный ID либо текущий
валидный callback. Runtime.telegram получает опциональный answerCallback;
старые адаптеры остаются совместимыми для прежних действий. Outbox стал
размеченным объединением: callback_answer содержит callbackQueryId/showAlert,
а не chatId. LIVE-доставка выбирает answerCallbackQuery; ok обозначает запись
в outbox, не сетевую доставку. Поля симулятора и отображение уведомления
не требуют backend. Учебное доказательство запуска учитывает вид события.

**Последствия.** Формат проекта v1, старые провода и сообщения сохранены.
Нет нового транспорта/зависимостей/токена в браузере. Inline-mode, игры,
недоступные сообщения, конструктор клавиатуры и автоподтверждение вне C1.
Существующая отложенная доставка и её ограничения описаны в TELEGRAM.md.
Callback-данные не авторизуют бизнес-действия и не гарантируют одноразовость.


## ADR-026 — Проверяемая callback-клавиатура и адресация outbox (2026-09-15)

**Контекст.** C1 принимает/подтверждает callbacks, но не создаёт сообщения
с кнопками. Порт сообщения возвращает локальный номер, не ID Telegram;
одинаковые подписи не идентифицируют ни кнопку, ни исходящее сообщение.

**Решение.** Новый конструктор telegram.inline_keyboard: rows → keyboard,
настройка JSON. Белый список text/callback_data, локальные лимиты и общий
UTF-8 валидатор C1. Дополнительный необязательный keyboard у send_message;
разметка проходит outbox и серверный reply_markup с повторной проверкой.
Только callback-кнопки, без произвольных атрибутов и URL-действий.

Негативный тест выявил: ошибочный pull-источник оставляет inputs.keyboard
undefined, что могло превратиться в отправку без кнопок. В контекст обработчика
добавлены необязательные connectedInputs — общая метаинформация, без привязки
движка к Telegram. C2 отличает отсутствие провода от недоставленного значения
и отказывает во втором случае. Правила планирования и старые обработчики
не переписаны; ошибка источника не скрывается fallback-настройкой.

UI передаёт только UUID записи outbox и индексы кнопки. Стор проверяет
состав/свежесть/контекст/userId повторно, строит callback и синхронно очищает
старый результат началом запуска. Общий компонент Чат/Телефон, без прямого
Bot API. Локальный messageId лишь данные симуляции, не ключ идентичности.

**Последствия.** Схемы без keyboard совместимы, формат v1 неизменён.
Старые кнопки не переживают новый запуск/F5; transcript и сохранение
клавиатуры после callback не вводились. Надёжность доставки, реальные ID
исходящих сообщений, фото, inline-mode, URL/WebApp и автоподтверждение —
отдельные задачи. Практика/урок «Порты» приведены к новому входу.

## ADR-027 — Явные копии до серверного автосохранения (2026-09-15)

**Контекст.** Владелец принял серверное сохранение после плана 09. CRUD,
сессии и SQLite существуют, но Canvas использует синхронный LocalStorage
flush; PUT проекта безусловный, ревизий нет. Подмена глобального адаптера
привела бы к гонкам/потере правок и затронула бы учебные песочницы.

**Решение.** Ограниченный 10A: аккаунт/пространство и ручные новые копии в
Dashboard. POST с новым root ID, GET с валидацией → отдельный local ID.
Внутренние ID/модели/провода/группы/ссылки прежние; оригиналы не обновляются.
Песочницы отклоняются по самому документу, не только фильтром списка.

Узкий ProjectApi использует общий JSON-клиент, Zod и ожидание до 15 секунд.
Нет PUT/DELETE/LIVE и повторной записи при ошибке. Неподтверждённый POST
блокирует повтор до обновления списка; сервер мог успеть сохранить копию.
Локальный save должен успешно завершиться до подтверждения. Счётчик
запросов защищает восстановление от поздних ответов после ухода/смены
контекста. me/focus/BroadcastChannel обновляют аккаунт; канал не содержит
credentials, панели ботов/аудита перемонтируются. Новых зависимостей нет.

**Последствия.** Формат v1, автономный редактор, обучение и прогресс прежние.
Копии не связаны синхронизацией; локальные данные не разделены по аккаунтам.
Пароль не сохраняется, Vault/боты/версии/LIVE не копируются. Пользователь
видит ограничения и практику; это не объявляется полноценным серверным
редактором. Следующий 10B сначала вводит ревизии/условную запись (также
restore), потом отдельный режим редактора и обработку сети/конфликтов.
