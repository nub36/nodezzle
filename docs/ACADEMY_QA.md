# Академия — критический QA и исправление (2026-09-14, версия 0.5.27)

> Актуальный итог 0.5.38: **12/12 уроков имеют полное браузерное
> прохождение**, 441 unit/integration + 31 E2E. Итоговая таблица и границы
> покрытия — в разделе «07B4» в конце документа. Ранние разделы ниже
> сохраняют результаты соответствующих исторических версий.

Документ фиксирует результаты полного аудита цепочки «действие
пользователя → React Flow/сторы → движок урока» и введённые механизмы
защиты от регрессий. Продакшн обновляется владельцем самостоятельно:
рабочий коммит указан в конце документа.

## Найденный и исправленный баг

**Симптом**: урок «Знакомство с Canvas», шаг 4/5 «Выберите деталь
„Текст"»: клик по детали выделял её и открывал Инспектор, но шаг не
засчитывался — урок зависал.

**Корневая причина** (`src/academy/completion.ts`): правило
`select-block` сравнивало `snapshot.selectedBlockId` (это ТИП детали,
например `core.text` — снимок заполняется из `project-store`)
с идентификатором экземпляра узла на холсте (`node.id` вида `n1`).
Сравнение типа с экземпляром не совпадает никогда; Инспектор при этом
работал, потому что читает другой источник данных, и маскировал баг.

**Исправление**: `select-block` сверяет тип детали
(`snapshot.selectedBlockId === step.blockId`). Это не точечный фикс
одного `if`: вся цепочка клик → выделение → снимок → правило аудирована
и покрыта регрессионными и интеграционными тестами.

### Побочные находки аудита (тоже исправлены)

| Находка | Исправление |
| --- | --- |
| `connect` засчитывал соединение без ручек портов или с неверным портом | требуемые порты обязаны совпадать точно |
| `run`/`send-simulator-message` засчитывали старый запуск, сделанный до появления шага | требуется НОВОЕ выполнение (`run.at >= stepStartedAt`) |
| события для диагностики нигде не фиксировались | введён журнал нормализованных событий моста |

## Единый Completion Bridge

Второго параллельного рантайма нет: единственный источник истины —
сторы приложения, из которых собирается снимок
(`src/features/academy/snapshot.ts` → `AcademySnapshot`), а движок
(`src/academy/completion.ts`) проверяет шаг по снимку.

Нормализованные события (`src/academy/events.ts`, выводятся дифом двух
снимков, пишутся в журнал урока — `recentEvents`, последнее —
`lastEvent`):

| Событие | Данные |
| --- | --- |
| `BLOCK_ADDED` | nodeId, blockType |
| `BLOCK_REMOVED` | nodeId, blockType |
| `BLOCK_SELECTED` | nodeId, blockType |
| `CONNECTION_CREATED` | nodeId-концы, блок-типы и порты обоих концов |
| `BLOCK_CONFIG_CHANGED` | nodeId, blockType, ключ настройки |
| `EXECUTION_FINISHED` | executionId, status, source |
| `SIMULATOR_MESSAGE_SENT` | executionId, текст |
| `MODEL_CREATED` | modelId, blockType `models.call` |
| `DEBUG_OPENED` / `DEBUG_CLOSED` | — |
| `PAGE_OPENED` | route |

## Источники истины по типам шагов

| Тип шага | Источник истины | Событие/условие завершения | Семантика «уже выполнено» |
| --- | --- | --- | --- |
| `information` | интерфейс (кнопка «Понятно») | `acknowledge()` | — |
| `open-page` | роутер (`location.pathname`) | маршрут совпал | засчитывается по состоянию |
| `add-block` | `project-store.nodes` | деталь типа `blockId` на холсте | засчитывается по состоянию |
| `select-block` | `project-store.selectedNodeId` → тип детали | тип выбранной детали == `blockId` | засчитывается по состоянию |
| `connect` | `project-store.edges` | ребро нужных типов И портов, направление учтено | засчитывается по состоянию |
| `configure` | `config` узла в `project-store` | ключ заполнен (`notEmpty`) или равен `equals` | засчитывается по состоянию |
| `run` | `execution-store.history` | последнее выполнение успешно/завершено | **только НОВОЕ** после начала шага |
| `send-simulator-message` | `execution-store.history` + текст симулятора | успешный запуск с нужным `source` (+ подстрока) | **только НОВОЕ** после начала шага |
| `create-model` | узлы `models.call` и модели проекта | вызов ссылается на существующую модель | засчитывается по состоянию |
| `open-debug` | `tutorial-store.debugOpen` | панель отладки открыта | засчитывается по состоянию |
| `quiz` | интерфейс (варианты ответа) | верный вариант через `answerQuiz()` | — |

Неверное действие не засчитывается: выбор другой детали, соединение не
теми портами/деталями, старый запуск — все проверены тестами.

## Оверлей и навигация

- Затемнение — `pointer-events: none`, клики по холсту и библиотеке
  не блокируются; карточка урока — единственный перехватывающий элемент.
- Каждый шаг несёт `target` (валидируется: только известные зоны).
- Кнопка «Проверить шаг» (`tutorial-recheck`) — фолбэк: просто
  перевыполняет автоматическую проверку, автопроверка работает всегда.
- После последнего шага — экран «Урок завершён» с кнопками
  «Следующий урок» / «В Академию» / «Остаться» и «Повторить».
- Прогресс сохраняется в `localStorage`; перезагрузка страницы посреди
  урока восстанавливает тот же шаг (проверено тестом).

## Диагностика (только для разработки)

Добавьте `?academyDebug=1` к адресу: панель `tutorial-debug` показывает
урок, шаг, его тип и последнее событие моста с данными. В продуктовой
сборке панель не нужна — включается только параметром адреса.

## Автоматическое покрытие

| Слой | Файл | Что доказывает |
| --- | --- | --- |
| Юнит правил | `src/academy/academy-engine.test.ts` | семантика каждого типа шага, включая регрессию `select-block` |
| Мост и интеграция | `src/academy/completion-bridge.test.ts` | 14 сценариев: реальные сторы → снимок → шаг; неверные действия не засчитываются; журнал событий |
| Статический валидатор | `src/academy/qa-manifest.test.ts` + `validate-deep.ts` | порты/детали/настройки/маршруты/цели существуют; невозможные шаги в уроках без песочницы отклоняются; манифест 12 уроков |
| Обход курса | `src/academy/lesson-walkthrough.test.ts` | каждый шаг каждого из 12 уроков выполним; уроки доходят до `finished` |
| Браузерный E2E | `e2e/academy.spec.ts` (Playwright) | 4 сценария критического пути по `data-testid` |

### Стабильные data-testid (без поиска по классам/тексту)

`lesson-card-<id>`, `lesson-start`, `lesson-restart`, `tutorial-card`
(+ `data-step-id`, `data-step-kind`), `tutorial-step-index`,
`tutorial-ack`, `tutorial-recheck`, `tutorial-exit`,
`tutorial-quiz-<optId>`, `tutorial-quiz-wrong`, `tutorial-finished`,
`tutorial-next-lesson`, `tutorial-to-academy`, `tutorial-stay`,
`tutorial-restart`, `tutorial-debug`, `library-search`,
`library-category-<категория>`, `library-item-<id>`,
`canvas-node-<blockId>`, `run-button`, `onboarding-skip`.

## Браузерный E2E: статус выполнения

**2026-09-14, версия 0.5.29: 11/11 браузерных тестов реально пройдены в
этой сессии**, Chromium 153.0.8010.0 + Playwright. Прямое скачивание с CDN
по-прежнему завершалось `ECONNRESET`. Для прогона использована внешняя
установка `@sparticuz/chromium` (не зависимость проекта, не входит в Git).
В конфиг добавлен необязательный `PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH`.

Обычный локальный запуск:

```bash
npm run test:e2e:install
npm run test:e2e
```

Для подготовленного системного Chromium:

```bash
PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=/path/to/chromium npm run test:e2e
```

В этой среде дополнительно требовался `LD_LIBRARY_PATH=/tmp/al2023/lib`
для библиотек внешней сборки; эти временные пути не являются частью продукта.

### Что выявил фактический браузерный запуск

- Старые E2E пытались открыть заблокированный урок, очищали LocalStorage
  при F5, искали неверное имя конвертера и сворачивали уже раскрытые
  категории. Фикстуры исправлены: prerequisites заданы, F5 сохраняет
  прогресс, поиск по реальному ID, соединяемые детали раздвигаются
  пользовательским перетаскиванием.
- В продукте шаг выбора проскакивал из-за автовыделения вставленного узла.
  Подготовка учебного шага снимает это выделение один раз до автопроверки.
- Абсолютные маркеры портов накладывались; теперь каждый находится в
  своей строке, обе колонки независимы.

### Покрытие 0.5.29

| Набор | Факт |
|---|---|
| `academy.spec.ts` | 4 сценария: правильный/неправильный выбор, ручная перепроверка, F5; также проверено отсутствие перекрытия инспектора |
| `connection.spec.ts` | 3 сценария: совместимое/несовместимое соединение и переход учебного шага |
| `compact-layout.spec.ts` | 4 сценария: раскрываемый план с клавиатуры, сворачивание и теория, экран 390×640, размеры и отдельные маркеры портов |
| `npm test` | 395/395, 52 файла, включая статическую проверку и обход снимков всех 12 уроков |

Это **не** полный браузерный обход каждого шага всех 12 уроков. Его
расширение — следующий подэтап 07B; не заменять эту оговорку обещанием
полного E2E-покрытия курса.

## Итог проверки 12 уроков

Все 12 уроков проходят углублённую валидацию и полный обход без
зависаний (см. тесты выше). Нереализуемых интерактивных шагов в курсе
нет: каждый шаг имеет источник истины и снимок-выполнитель. Уроки без
песочницы (`intro-what`, `publish-versions`) содержат только чтение,
викторины и просмотр — это зафиксировано валидатором.

## Коммиты задачи

- **A** — Completion Bridge + фикс `select-block` + тесты;
- **B** — углублённый валидатор + QA-манифест;
- **C** — браузерный E2E (Playwright);
- **D** — обход 12 уроков, документация, версия 0.5.27.

Рабочая ветка: `arena/01a0a0c0-nodezzle` (финальный коммит указан в
истории ветки; продакшн обновляет владелец).

## 07B1 — конвертеры и эхо-бот (0.5.30)

**Реальный прогон 2026-09-14: 14/14 E2E, 402/402 unit/integration
(53 файла), typecheck и build успешны.** Окружение браузера — то же,
Chromium 153.0.8010.0, внешний путь через переменную конфигурации.

`e2e/practice-lessons.spec.ts` добавляет три сценария:

1. Конвертеры — каждый шаг, отклонение значения «41», текст «42» на входе,
   число 42 на выходе и в логе; закрытие панели/неверная вкладка не
   завершают просмотр; правильная вкладка завершает урок, прогресс переживает F5.
2. Эхо-бот — каждый шаг, неправильный совместимый `user_id` не принимается
   за `chat_id`; после переподключения ответ содержит точный текст и ID
   заданного чата; викторина отвергает хранение токена в проекте.
3. Конвертеры после F5 — схема с двумя связями и значение восстановлены;
   «не число» вызывает ошибку без завершения шага; исправление на «42»
   даёт успешный результат и позволяет завершить урок.

Все действия — клики, ввод и перетаскивания, без подмены снимков или
результатов выполнения. В фикстуре задаются только завершённые предпосылки,
но не целевой урок. Результаты читаются из отрисованных портов и outbox-чата.

**До исправления** первые два сценария падали на шаге просмотра:
конвертеры сразу показывали «Урок завершён», эхо-бот переходил к викторине.
Причина — `debugOpen === true` у уже открытого симулятора и автопереключение
вкладки наблюдателем. Исправление: необязательный `tab`, его проверка и
отключение автопоказа для такого шага. `debug-step.test.ts` покрывает
правило, старое поведение, валидатор, снимок, мост событий и перепроверку.

Подсветка проверена по геометрии: кнопка 🐞 при закрытой панели, вкладка
чата при открытой. Новые стабильные маркеры: `debug-tab-*`, `debug-toggle`,
`debug-node-<blockId>` с ID экземпляра, `input-<port>` / `output-<port>`,
`simulator-chat-user`, `simulator-chat-bot`, `tutorial-highlight`.

**Граница покрытия на 0.5.30:** весь 07B ещё не завершён. Продолжение
с условиями и командами Telegram описано ниже; затем веб, модели и отладка.
Доступная вкладка — проверяемое состояние, а не доказательство того, что
человек прочёл данные; нового клика при уже открытой нужной вкладке правило
не требует. Ошибочный запуск при этом проверяется отдельным шагом `run`.


## 07B2 — условия и команды Telegram (0.5.31)

**Реальный прогон 2026-09-14: 17/17 E2E (1 минута), 412/412 unit/integration
(54 файла), typecheck, проверка типов E2E и build успешны.** Браузер —
внешний Chromium, прежняя конфигурация; новых зависимостей нет.

`e2e/branches-commands.spec.ts` проходит через реальный UI:

1. Условие: неверные оператор/пустая цель не принимаются; подключение
   «Нет» вместо «Да» не завершает шаг. Ошибочная связь отменяется кнопкой
   undo. Сообщение «добрый день» даёт только `false`, лог не получает
   входа и задание остаётся. Простая правка текста не засчитывается;
   новый запуск с «привет, NODEZZLE» даёт `true` и тот же текст в логе.
   Проверены оба ответа викторины и сохранение завершения после F5.
2. Команда: готовая настройка /start принимается автоматически; `args`
   не заменяет требуемый `command`, неверная связь отменяется. /help не
   даёт ответа и не завершает шаг; /start даёт ровно один ответ /start
   в чат 13579. «Порты» не заменяют просмотр «Чата Telegram».
3. Подмена настройки: /help в самой детали вместе с /help в симуляторе
   действительно отправляет /help, но цель урока не выполнена. F5
   сохраняет две связи, настройку и шаг; исправление на /start и новый
   запуск позволяют завершить урок. Завершение тоже сохраняется после F5.

Общая подготовка вынесена в `e2e/lesson-helpers.ts`: задаются только
предпосылки, целевой урок не подменяется. Узлы раздвигаются мышью, связи
создаются через порты; результаты читаются из отрисованных портов/чата.

**Красные проверки до исправления:** условие переходило к викторине при
`false`; команда сразу завершала урок вместо шага просмотра чата.
Исправлены требования выходов `expectedOutputs` и `open-debug.tab` у
команды. Дополнительно исключена подмена результатов изменённой формой:
источник/текст записываются при запуске, выходы связаны с ID исполнения.
Десять новых тестов в `run-evidence.test.ts` проверяют также пропущенные,
ошибочные, устаревшие и допустимые ложные/нулевые значения.

Инструкции используют реальные названия «Да»/«Нет», «Сравнивать с»,
предупреждают о регистре и отдельном поле команды. Существующее значение
/start не требует искусственного клика. ID/число/порядок шагов не менялись.

**Граница покрытия:** полный браузерный обход есть у пяти интерактивных
уроков: Canvas, конвертеры, эхо-бот, условия, команды. Все 12 имеют
статическую валидацию и синтетический обход снимков, но это не полный E2E
курса. Продолжение 07B3 описано ниже; весь 07B остаётся открытым.


## 07B3 — веб, модели и отладка (0.5.32)

**Фактический прогон 2026-09-14: 426/426 unit/integration (56 файлов),
22/22 E2E (1,4 минуты), typecheck, типы E2E и build успешны.** Браузер —
Chromium 153.0.8010.0 из внешней установки; новые зависимости не добавлены.

Пять сценариев `e2e/web-models-debug.spec.ts`:

1. Веб-превью: загрузка исполняет страницу, но не кнопку/лог и не
   завершает задание; клик даёт `button_click`, данные кнопки совпадают
   с входом лога. Просмотр журнала не заменяет требуемые «Порты».
2. Модель: выделение Ctrl, реальное извлечение через контекстное меню и
   диалог. На внешнем холсте остаётся один вызов; F5 сохраняет модель и
   шаг. Журнал содержит «Внутри модели: 73», результат пустого контракта
   равен `{}`. Двойной клик открывает две детали с одной связью.
3. Отладка: «Отладка: 123» в журнале и портах, одна успешная запись
   локальной истории; каждый просмотр требует свою вкладку. Неверный
   ответ викторины не принимается, верный завершает урок.
4. Ошибка модели: внутрь добавляется преобразование нечислового текста.
   Внешний вызов имеет статус error и не выдаёт result, урок остаётся
   на запуске. Удаление ошибочного преобразования и новый запуск помогают.
5. История: 30 настоящих запусков до учебного шага, затем сборка цепочки
   и 31-й запуск. Зачёт автоматический, без ручной перепроверки/смены
   вкладки; история по-прежнему ограничена 30 строками.

Завершение всех трёх уроков проверяется после F5. Целевой прогресс не
подменяется, фикстуры задают только предпосылки. Маркеры
`execution-log-entry` и `local-history-entry` читают отрисованные данные.

**Красные проверки:** исходно page_load отдавался также кнопкой;
инструкция Shift не выделяла две детали; шаг открытия журнала
пропускался; ошибка внутренней модели была видна снаружи как success.
Каждая из этих проблем воспроизведена браузером до исправления.

Дополнительно: правило создания модели сверяет её существование;
запуски требуют выходы, соединения — оба порта. Наблюдатель подписан на
ID последнего исполнения. 14 новых unit/integration покрывают веб-типы,
общий старый запуск без event, неподходящий источник/событие, историю,
многоуровневые ошибки моделей, пустой контракт, снимок и учебные правила.
Старый тест клика по схеме из одной страницы исправлен на page_load;
неподходящий клик отдельно проверяется как ожидание, а не успех.

**Граница покрытия:** полное E2E-прохождение есть у 8 из 12 уроков:
Canvas, условия, конвертеры, эхо-бот, команды, веб, модели, отладка.
07B4 должен завершить `intro-what`, `basics-ports`, `basics-chain`,
`publish-versions`. У всех 12 сохранены статическая валидация и обход
синтетических снимков; они не подменяют браузерный прогон.


## 07B4 — оставшиеся уроки и закрытие 07B (0.5.33)

**Фактические проверки 2026-09-14: 430/430 unit/integration (57 файлов),
27/27 E2E (1,6 минуты), typecheck, типы E2E и build успешны.** Chromium
153.0.8010.0 установлен вне репозитория, новые зависимости не добавлены.

Пять новых сценариев `e2e/foundation-lessons.spec.ts`:

1. `intro-what`: оба объяснения, дополнительная теория, неверные/верные
   ответы двух викторин, F5 посреди урока. Проект не создаётся.
2. `basics-ports`: реальные два входа отправки и отсутствие входов у текста,
   доступные названия/типы портов и класс квадратного маркера ошибки.
   Текст → числовой ID чата отклоняется, текст → текст соединяется.
   F5 сохраняет связь и текущий шаг, викторина проверяется обоими ответами.
3. `basics-chain`: неверный совместимый `chat_id` не подменяет `text`;
   после отмены подключается правильный порт. F5 сохраняет схему и шаг;
   сообщение «Первый запуск: привет, 42!» видно на выходе триггера,
   входе лога и в журнале. «Порты» не заменяют просмотр журнала.
4. `basics-chain`, отрицательный: связь удаляется перед запуском. Успех
   триггера не завершает задание; восстановить связь без нового запуска
   недостаточно. Повторное выполнение позволяет закончить урок.
5. `publish-versions`: объяснение границ визуального режима, F5, подтверждение
   теории и викторина. Сохранённый рабочий проект побайтно неизменен,
   новых проектов нет, запросов записи API нет. Такие запросы дополнительно
   блокируются тестом, чтобы даже при регрессии не выполнять публикацию.

Во всех новых сценариях завершение проверяется после перезагрузки.
**До исправления** оба сценария первой цепочки падали: общий success
засчитывался без передачи в лог, просмотр журнала пропускался. Исправления
используют уже существующий контракт движка. Четыре новых unit-теста
проверяют порты, результаты/время, вкладку и теоретические границы.

### Итоговая карта полного прохождения — 12/12

| Урок | Браузерный файл | Проверяемый итог |
|---|---|---|
| `intro-what` | `foundation-lessons.spec.ts` | Объяснения, обе викторины, без проекта |
| `intro-canvas` | `academy.spec.ts` | Вставка, ручной выбор детали, инспектор, завершение |
| `basics-ports` | `foundation-lessons.spec.ts` | Входы/выходы, совместимость, ошибка, викторина |
| `basics-chain` | `foundation-lessons.spec.ts` | Точный текст в цепочке и журнале, отсутствие связи и исправление |
| `logic-condition` | `branches-commands.spec.ts` | Фактические false/true, лог, викторина |
| `data-converters` | `practice-lessons.spec.ts` | Текст 42 → число 42, ошибка/исправление, F5 |
| `telegram-first-bot` | `practice-lessons.spec.ts` | Один ответ с точным текстом и ID чата |
| `telegram-command` | `branches-commands.spec.ts` | /start, игнорирование/подмена команды, чат, F5 |
| `web-first-page` | `web-models-debug.spec.ts` | Событие загрузки не подменяет клик, данные кнопки в логе |
| `models-first-model` | `web-models-debug.spec.ts` | Извлечение, внутренний лог, спуск, ошибка/исправление, F5 |
| `debug-why-not-working` | `web-models-debug.spec.ts` | Журнал/порты/история, автоматический 31-й запуск |
| `publish-versions` | `foundation-lessons.spec.ts` | Теория без публикации и изменения рабочего проекта |

**07B закрыт. Границы результата:** это отдельные полные сценарии каждого
урока с подготовленными предпосылками, не один непрерывный пользовательский
маршрут от нулевого прогресса до конца курса. Проверены Chromium и выбранные
сценарии ошибок; это не все браузеры, варианты схем или полная мобильная
адаптация Canvas. Теоретическая публикация не подтверждает боевой деплой.
Следующий подэтап — 07C: вставка деталей без наложений.


## Регрессия курса после свободной вставки (0.5.34, 07C)

**2026-09-14: 441/441 unit/integration (58 файлов), 31/31 E2E (1,7 минуты),
typecheck, типы E2E и build успешны.** Все ранее добавленные сценарии
12 уроков прошли с новой логикой вставки. Новых зависимостей нет.

`e2e/insertion.spec.ts` добавляет четыре сценария:

1. Разные высоты и повторная вставка: прямоугольники не пересекаются,
   координаты старых узлов неизменны; undo/redo и F5 сохраняют позиции.
2. Увеличенный масштаб и открытая отладка: серия вставок, превышающая
   вместимость экрана; новая деталь видна между панелями, старые позиции
   сохраняются, даже если камера смещается.
3. Настоящий HTML drag & drop: свободная точка совпадает с курсором
   (погрешность до 1 px), повторный drop в занятое место обходится без наложения.
4. Соединение протянуто в пустоту: меню создаёт «Лог» на свободном месте,
   автоматически соединяет порты; запуск доставляет «Быстрая вставка: 42».

Первый тест до исправления воспроизводил наложение. Проверка F5 выявила
округление дробных координат существующим сериализатором — новая вставка
выбирает целые позиции заранее, не меняя формат. 11 unit-тестов покрывают
геометрию, размеры до измерения, узкие промежутки, границы, переполнение,
80 последовательных вставок и 1000 далёких препятствий (не нагрузочный
браузерный бенчмарк). Подробные ограничения поведения — в UI.md.


## Регрессия после копирования фрагментов (0.5.35, 08A)

Полный набор: **454/454 unit/integration, 35/35 E2E**. Все 12 отдельных
полных сценариев уроков повторно пройдены. Учебные тексты, прогресс и
проверки шагов в этом подэтапе не менялись. Четыре новых сценария
`e2e/clipboard.spec.ts` проверяют дублирование, повторную вставку,
фактические значения внутренних связей, независимость настроек,
undo/redo/F5, видимость при масштабе и открытой отладке, контекстное меню
и работу сочетаний в поле ввода. Мобильные панели остаются задачей 08B.


## Поправки по скриншоту владельца (0.5.36)

На скриншоте — «Первая цепочка», шаг соединения, окно поверх редактора,
быстрая вставка и провод от user_id, хотя инструкция требует text.
Неверный порт не должен засчитываться. Выявлены проблемы взаимодействия:
карточка перехватывала отпускание над закрытым ею портом, ручная проверка
не давала обратной связи, Escape меню доходил до подтверждения выхода
из урока. Все три пути исправлены без обхода критериев зачёта.

**471/471 unit/integration, 49/49 E2E**, типы/сборка успешны. В полном
браузерном наборе повторно пройдены 12 отдельных полных сценариев уроков.
Дополнительно `tutorial-window.spec.ts` содержит 14 сценариев:

- 1280×900, увеличенный масштаб, неверный user_id → log.value, лишняя
  security.mask: шаг остаётся connect, ручная проверка называет text/value;
  меню закрываются Escape без прерывания; окно переносится прямо поверх
  входа, правильный реальный провод проходит сквозь него и меняет шаг;
  после F5 сохраняются позиция окна/связь/шаг, запуск доставляет
  «Исправлено: 42», просмотр журнала завершает урок.
- Стрелки/Shift, перенос за края, resize до 390×640, сворачивание и сброс.
- По одному сценарию на каждый из 12 уроков: перенос не меняет шаг,
  доступная ручная проверка показывает результат, F5 сохраняет положение,
  Home возвращает исходное. Это проверки окна, не замена полных сценариев.

`connect-recovery.test.ts` проверяет все 12 connect-шагов каталога:
неверные порты и ручная перепроверка не завершают шаг, исправление с
оставленной лишней деталью продвигает ровно на следующий шаг.
`floating-window.test.ts` — 5 проверок границ. Touch-жесты на реальном
телефоне, серверная версия сайта владельца и все размеры экранов не
проверялись; мобильные панели — отдельный 08B.


## Адаптивные панели (0.5.37, 08B)

**474/474 unit/integration, 52/52 E2E**. Все 12 отдельных полных уроков
на настольном экране и 14 сценариев исправлений 0.5.36 повторно пройдены.
`narrow-canvas.spec.ts` добавляет два сценария редактора (390/768×800):
панели, отсутствие горизонтального переполнения страницы, фокус, вставка,
значение настройки и реальный выход запуска, Escape/крестик, инструменты,
отладка, F5 и возврат к 1440 px без изменения позиции узла.
Третий сценарий полностью проходит `intro-canvas` на 390 px, включая
ручное открытие библиотеки/инспектора, реальный выбор узла и Escape без
прерывания. Остальные 11 уроков на телефоне и сенсорные соединения этим
набором не проверены. Новые 3 unit подтверждают состояние панели и persist.


## Регрессия после адресных Web-событий (09A, 0.5.38)

**493/493 unit/integration, 55/55 E2E**. Все 12 настольных уроков,
исправления окна, панели 390/768 px и вводный урок на 390 px повторно
пройдены. Тексты/критерии уроков не менялись. `web-events.spec.ts` добавляет
два независимых обработчика с одинаковой подписью: клик и submit передают
данные лишь своей цепочке, F5 сохраняет адреса узлов; ошибки формы
блокируют отправку. Загрузка/перезагрузка двух страниц запускает первую
показанную, не обе. Это проверка событий, не полного Web-рендеринга.
