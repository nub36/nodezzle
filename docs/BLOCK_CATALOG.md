# BLOCK_CATALOG — Каталог деталей NODEZZLE

**Обновлено:** 2026-09-14 (Этап 4, часть A). Развивает [BLOCKS.md](BLOCKS.md) в полный
каталог с фактическими статусами. Источник правды о факте — реестр
(`src/blocks/`, автотест `src/blocks/library-catalog.test.ts`).

## 1. Статусы определений

Каждое определение несёт статус (поле `status`, назад совместимое; вывод —
`effectiveStatus()` реестра):

| Статус | Значение |
|---|---|
| `implemented` (РЕАЛИЗОВАНО) | есть исполняемый код (`runtime`), деталь доступна в палитре |
| ПОКРЫТО ТЕСТАМИ | реализовано и непосредственно исполняется автотестами (уточнение к РЕАЛИЗОВАНО) |
| `planned` (ЗАПЛАНИРОВАНО) | определение и контракт портов зафиксированы, исполнение не реализовано; скрыто из палитры (`available: false`) |
| `prototype` | черновая реализация, поведение может меняться |
| `experimental` | работает, но контракт нестабилен |
| `deprecated` | устарело, будет убрано в следующих версиях |
| СЛУЖЕБНОЕ | не в палитре; вставляется особым действием (стикер) |

**Факт на дату (часть A завершена):** зарегистрировано **65** определений —
**50 в палитре** (все с исполняемым кодом, из них 14 покрыты исполнением
в тестах), **14 запланированных** (скрыты из палитры) и **1 служебное**
(стикер-заметка, тоже скрыт из палитры). Целевая библиотека Этапа 4 — 16 категорий × 10+ блоков
(160+ определений, [04-BLOCK-LIBRARY.md](agent-plan/04-BLOCK-LIBRARY.md)).
Запланированная деталь — это определение и место в каталоге; она **не** считается реализованной.

## 2. Сводка по категориям (после части A)

| Категория | Определений | Из них в палитре | Исполняемых |
|---|---|---|---|
| Ядро `core` | 10 | 9 | 9 |
| Логика `logic` | 10 | 10 | 10 |
| Данные `data` | 10 | 10 | 10 |
| Поток `flow` | 13 | 4 | 4 |
| Конвертеры `converters` | 7 | 7 | 7 |
| Telegram-события `telegram_events` | 2 | 2 | 2 (транспорт — симуляция) |
| Telegram-действия `telegram_actions` | 2 | 2 | 2 (outbox-симуляция) |
| Веб-события `web_events` | 3 | 3 | 3 (события превью) |
| Модели `models` | 1 | 1 | 1 |
| Память `memory` | 2 | 0 | 0 |
| HTTP `http` | 1 | 0 | 0 |
| Дата/время `datetime` | 1 | 1 | 1 |
| Отладка `debug` | 1 | 1 | 1 |
| AI `ai` | 1 | 0 | 0 |
| Заметки `notes` | 1 | 0 (служебная) | 1 (вне выполнения) |
| **Всего** | **65** | **50** | **49** |

Части Этапа 4: **A** (ядро/логика/данные/поток — выполнено), **B** —
Telegram, **C** — веб-интерфейс и веб-события, **D** — модели и память,
**E** — HTTP и дата/время, **F** — файлы/медиа и безопасность, **G** —
отладка и конвертеры, **H** — финальный аудит (16 × 10+).

## 3. Ядро (core) — 10

| Деталь | ID | Порты | Конфигурация | Статус |
|---|---|---|---|---|
| Текст | `core.text` | → Текст | `value` | ПОКРЫТО ТЕСТАМИ |
| Число | `core.number` | → Число | `value` | РЕАЛИЗОВАНО |
| Логическое | `core.boolean` | → Логическое | `value` | РЕАЛИЗОВАНО (часть A) |
| Массив | `core.array` | → Массив | `value` (JSON) | РЕАЛИЗОВАНО (часть A) |
| Объект | `core.object` | → Объект | `value` (JSON) | РЕАЛИЗОВАНО (часть A) |
| JSON | `core.json` | → JSON | `value` | РЕАЛИЗОВАНО |
| Ссылка | `core.url` | → Текст | `value`, нормализация | РЕАЛИЗОВАНО (часть A) |
| Шаблон | `core.template` | ← Любой (входы-заполнители) → Текст | `template` | ЗАПЛАНИРОВАНО |
| Вход модели | `core.input` | → Любой (entry) | `portId` | ПОКРЫТО ТЕСТАМИ |
| Выход модели | `core.output` | ← Любой | `portId` | ПОКРЫТО ТЕСТАМИ |

## 4. Логика (logic) — 10

| Деталь | ID | Порты | Конфигурация | Статус |
|---|---|---|---|---|
| Условие | `logic.condition` | ← Любой → Истина/Ложь | `operator, target, trueValue, falseValue` | ПОКРЫТО ТЕСТАМИ |
| Переключатель | `logic.switch` | ← Любой → ветки | правило выбора | РЕАЛИЗОВАНО |
| Сравнение | `logic.compare` | ← Лево, Право → Результат | `operator` (8 операций) | РЕАЛИЗОВАНО (часть A) |
| И | `logic.and` | ← A, B → Результат | — | РЕАЛИЗОВАНО (часть A) |
| ИЛИ | `logic.or` | ← A, B → Результат | — | РЕАЛИЗОВАНО (часть A) |
| НЕ | `logic.not` | ← Значение → Результат | — | РЕАЛИЗОВАНО (часть A) |
| ЛИБО (исключающее ИЛИ) | `logic.xor` | ← A, B → Результат | — | РЕАЛИЗОВАНО (часть A) |
| Пусто? | `logic.is_empty` | ← Значение → Логическое | — | РЕАЛИЗОВАНО (часть A) |
| В диапазоне | `logic.in_range` | ← Значение → Логическое | `min, max, includeBounds` | РЕАЛИЗОВАНО (часть A) |
| Первое значение | `logic.coalesce` | ← A, B → Результат | — | РЕАЛИЗОВАНО (часть A) |

## 5. Данные (data) — 10

| Деталь | ID | Порты | Конфигурация | Статус |
|---|---|---|---|---|
| Поле объекта | `data.object_get` | ← Объект → Значение (+Ошибка) | `path`, `fallback` | РЕАЛИЗОВАНО (часть A) |
| Установить поле | `data.object_set` | ← Объект, Значение → Объект | `path` | РЕАЛИЗОВАНО (часть A) |
| Ключи объекта | `data.object_keys` | ← Объект → Массив | — | РЕАЛИЗОВАНО (часть A) |
| Объединить объекты | `data.object_merge` | ← A, B → Объект | — | РЕАЛИЗОВАНО (часть A) |
| Элемент массива | `data.array_get` | ← Массив → Значение (+Ошибка) | `index` | РЕАЛИЗОВАНО (часть A) |
| Добавить в массив | `data.array_add` | ← Массив, Элемент → Массив | `position` | РЕАЛИЗОВАНО (часть A) |
| Длина массива | `data.array_length` | ← Массив → Число | — | РЕАЛИЗОВАНО (часть A) |
| Соединить в текст | `data.array_join` | ← Массив → Текст | `separator` | РЕАЛИЗОВАНО (часть A) |
| Срез массива | `data.array_slice` | ← Массив → Массив | `start, end` | РЕАЛИЗОВАНО (часть A) |
| Сортировка | `data.array_sort` | ← Массив → Массив | `order, key` | РЕАЛИЗОВАНО (часть A) |

## 6. Поток (flow) — 13

| Деталь | ID | Порты | Конфигурация | Статус |
|---|---|---|---|---|
| Задержка | `flow.delay` | ← Любой → Любой (+Ошибка) | `delayMs` | ПОКРЫТО ТЕСТАМИ |
| Заслонка | `flow.gate` | ← Значение, Условие → Результат | — | РЕАЛИЗОВАНО (часть A) |
| Разветвитель | `flow.split` | ← Значение → Ветка A, Ветка B | — | РЕАЛИЗОВАНО (часть A) |
| Объединитель | `flow.merge` | ← A, B → Результат | — | РЕАЛИЗОВАНО (часть A) |
| Стоп | `flow.stop` | ← Значение → — | `reason` | ЗАПЛАНИРОВАНО |
| Повтор | `flow.retry` | ← Любой → Любой (+Ошибка) | `attempts, delayMs` | ЗАПЛАНИРОВАНО |
| Антидребезг | `flow.debounce` | ← Любой → Любой | `delayMs` | ЗАПЛАНИРОВАНО |
| Ограничитель частоты | `flow.throttle` | ← Любой → Любой | `intervalMs` | ЗАПЛАНИРОВАНО |
| Очередь | `flow.queue` | ← Любой → Любой | `size` | ЗАПЛАНИРОВАНО |
| Интервал | `flow.interval` | триггер → Тик | `intervalMs` | ЗАПЛАНИРОВАНО |
| Обработчик ошибок | `error.handle` | — | — | ЗАПЛАНИРОВАНО (зарезервировано) |
| Отправить событие | `event.send` | — | — | ЗАПЛАНИРОВАНО (зарезервировано) |
| Получить событие | `event.receive` | — | — | ЗАПЛАНИРОВАНО (зарезервировано) |

## 7. Конвертеры (converters) — 7

Ранее жили в категории «Данные»; в части A выделены в отдельную категорию
`converters` (преобразование типов — самостоятельная роль, не хранение данных).

| Деталь | ID | Связь | Статус |
|---|---|---|---|
| Текст → Число | `data.text_to_number` | Текст → Число (+Ошибка) | ПОКРЫТО ТЕСТАМИ |
| Число → Текст | `data.number_to_text` | Число → Текст (+Ошибка) | ПОКРЫТО ТЕСТАМИ |
| Текст → Логическое | `data.text_to_boolean` | Текст → Логическое (+Ошибка) | РЕАЛИЗОВАНО |
| Текст → JSON | `data.text_to_json` | Текст → JSON (+Ошибка) | РЕАЛИЗОВАНО |
| JSON → Текст | `data.json_to_text` | JSON → Текст (+Ошибка) | РЕАЛИЗОВАНО |
| В объект | `data.to_object` | Любой → Объект (`key`) | ПОКРЫТО ТЕСТАМИ |
| Из объекта | `data.from_object` | Объект → Любой (`key`, +Ошибка) | ПОКРЫТО ТЕСТАМИ |

Идентификаторы сохранены (`data.*`) ради стабильности формата проектов;
категория определяет только положение в библиотеке.

## 8. Телеграм (16 категорий: события и действия)

Разделение категорий Этапа 4: `telegram_events` (триггеры) и
`telegram_actions` (действия). Часть B доведёт каждую до 10+.

### 8.1. События `telegram_events` — 2

| Деталь | ID | Тип | Порты | Статус |
|---|---|---|---|---|
| Получено сообщение | `telegram.message_received` | триггер | → Текст, ID пользователя, ID чата, Имя, Сообщение | РЕАЛИЗОВАНО (транспорт — симуляция) |
| Команда | `telegram.command` | триггер (`command`) | → Команда, Аргументы, ID пользователя, ID чата | ПОКРЫТО ТЕСТАМИ (симуляция) |

### 8.2. Действия `telegram_actions` — 2

| Деталь | ID | Порты | Статус |
|---|---|---|---|
| Отправить сообщение | `telegram.send_message` | ← Текст, ID чата → ID сообщения (+Ошибка) | ПОКРЫТО ТЕСТАМИ (outbox) |
| Отправить фото | `telegram.send_photo` | ← Фото, Подпись, ID чата → ID сообщения (+Ошибка) | РЕАЛИЗОВАНО (outbox) |

## 9. Веб (события и интерфейс)

### 9.1. События `web_events` — 3

| Деталь | ID | Тип | Порты | Статус |
|---|---|---|---|---|
| Страница | `web.page` | триггер | → Данные | ПОКРЫТО ТЕСТАМИ (события из превью/симулятора) |
| Кнопка | `web.button` | триггер | → Данные | РЕАЛИЗОВАНО |
| Форма | `web.form` | триггер | → Данные | РЕАЛИЗОВАНО |

### 9.2. Интерфейс `web_ui` — 0

Веб-элементы оформления (текст, заголовки, картинки, поля, окна) —
ЗАПЛАНИРОВАНО, часть C ([WEB.md](WEB.md)).

## 10. Модели (models) — 1

| Деталь | ID | Порты | Конфигурация | Статус |
|---|---|---|---|---|
| Вызов модели | `models.call` | ← Вход (объект) → Результат (объект), Ошибка | `modelId` | ПОКРЫТО ТЕСТАМИ (рекурсия покрыта) |

Часть D: подготовка входа, извлечение полей, значения по умолчанию,
валидация контракта, разбор ошибок модели и др.

## 11. Отладка (debug) — 1

| Деталь | ID | Порты | Конфигурация | Статус |
|---|---|---|---|---|
| Лог | `debug.log` | ← Любой → Любой | `label` | ПОКРЫТО ТЕСТАМИ |

Часть G: инспектор значений, утверждения, трассировка, счётчики и др.

## 12. Дата/время (datetime) — 1

| Деталь | ID | Порты | Статус |
|---|---|---|---|
| Текущее время | `datetime.now` | → Дата | РЕАЛИЗОВАНО |

Часть E: форматирование, арифметика дат, временные метки и др.

## 13. Память (memory) — 2 (запланированы)

| Деталь | ID | Статус |
|---|---|---|
| Переменная | `memory.variable` | ЗАПЛАНИРОВАНО |
| Общее хранилище | `memory.shared` | ЗАПЛАНИРОВАНО |

Часть D доведёт категорию до 10 (чтение/запись/счётчики/списки).

## 14. Остальные категории

| Категория | Определений | Состояние |
|---|---|---|
| Интернет-запросы `http` | 1 (`http.request`) | ЗАПЛАНИРОВАНО; часть E |
| Файлы и медиа `media` | 0 | ЗАПЛАНИРОВАНО; часть F |
| Безопасность `security` | 0 | ЗАПЛАНИРОВАНО; часть F |
| AI `ai` | 1 (`ai.text`) | ЗАПЛАНИРОВАНО; вне обязательных 16 |
| Заметки `notes` | 1 (`note.sticky`) | служебная; вне обязательных 16 |

## 15. Соответствие целевым 16 категориям Этапа 4

| Целевая категория (04) | Текущее состояние |
|---|---|
| Ядро | выполнено (10) |
| Логика | выполнено (10) |
| Данные | выполнено (10) |
| Поток | выполнено (13) |
| Конвертеры | 7, часть G доведёт до 10 |
| Телеграм-события | 2, часть B доведёт до 10 |
| Телеграм-действия | 2, часть B доведёт до 10 |
| Веб-события | 3, часть C доведёт до 10 |
| Веб-интерфейс | ЗАПЛАНИРОВАНО, часть C — 10 |
| Модели | 1, часть D доведёт до 10 |
| Память | 2, часть D доведёт до 10 |
| Интернет-запросы | 1, часть E доведёт до 10 |
| Дата/Время | 1, часть E доведёт до 10 |
| Файлы и медиа | ЗАПЛАНИРОВАНО, часть F — 10 |
| Безопасность | ЗАПЛАНИРОВАНО, часть F — 10 |
| Отладка | 1, часть G доведёт до 10 |

## 16. Механизмы библиотеки

- **Основные / Продвинутые**: у каждой детали поле `difficulty`
  (`basic`/`advanced`); вкладка «Основные» показывает только `basic`,
  «Все» — все доступные.
- **Поиск**: `matchesQuery()` ищет по русскому названию, описанию,
  идентификатору и `keywords`; новые детали участвуют автоматически.
- **Быстрая вставка**: кандидаты строятся функцией `quickInsertCandidates()`
  из общего реестра и системы типов портов — без отдельных каталогов.
- **Автотест каталога**: `src/blocks/library-catalog.test.ts` — уникальные
  ID, корректные порты и типы, статусы, сложность, русские названия,
  поиск и быстрая вставка. Полный порог «16 × 10+» добавляется в части H.

Правило добавления блока и контракт `BlockDefinition` — [BLOCKS.md](BLOCKS.md).
