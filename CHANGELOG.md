# CHANGELOG — NODEZZLE

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/).
Версия = `formatVersion` формата проекта + стабильность ядра.

## 0.5.42 — 2026-09-15 — Поля ввода и формы Web

- Закрыт 09B4: web.input/web.textarea реализованы отдельно и в деревьях.
  Настройки имени данных, экземпляра формы и начального значения;
  прежние входы подписи/подсказки и выход element сохранены.
- У формы явные режимы JSON/Поля. JSON по умолчанию сохраняет 09A;
  поля отправляют строки без смешивания с JSON и адресуют конкретный ID.
- Проверка состава/дублей/лимитов перед submit, блокировка неактуального
  дерева; неотправленные черновики не сохраняются после F5/размонтирования.
- Duplicate/paste поля с формой переназначает привязку на копию; отдельно
  скопированное поле теряет внешнюю привязку. Undo/redo проверены.
- 24 новых unit/integration + 5 E2E; всего **600/600 + 71/71**. Типы/сборка
  зелёные, все 12 уроков и предыдущие этапы сохранены. В палитре 93 детали
  из 165 определений. Практика/документация/ADR-023 обновлены.
- Формат v1, прогресс и транспорт прежние. Модальное окно, полная
  публикация Web и Telegram callback — следующие подэтапы. Деплоя нет.

## 0.5.41 — 2026-09-15 — Изображения и ссылки Web

- Закрыт 09B3: web.image/web.link реализованы с прежними ID/портами,
  настройками и инертными дескрипторами; отдельно и внутри структур B2.
- Единая HTTPS-валидация: DNS-host, без credentials/IP/локальных имён,
  нестандартных портов, исполняемых схем и неоднозначных символов; лимиты
  URL/подписей. DNS и редиректы не проверяются — это не сетевой sandbox.
- Внешние изображения загружаются только по кнопке, без referrer, с
  подписью/alt, ошибкой и повтором. Смена URL/контекста/F5 сбрасывает согласие.
  Ссылки открываются в новой вкладке с noopener/noreferrer.
- 42 новых unit/integration + 4 E2E; всего **576/576 + 66/66**. Типы/сборка
  зелёные, все 12 уроков и 09A/B1/B2/08B сохранены. 91 доступная деталь
  из 165 определений. Документация/учебная практика/ADR-022 обновлены.
- Формат v1, хранение и транспорт не менялись. Поля/модальные окна,
  полный Web-конструктор и публикация остаются в плане. Деплоя нет.

## 0.5.40 — 2026-09-14 — Контейнеры, секции и сетки Web

- Закрыт 09B2: существующие web.container/web.section/web.grid доступны,
  имеют runtime и безопасный рекурсивный вывод. Вход children остаётся
  массивом; порядок задаётся core.array/data.array_add, без неявной конверсии.
- Секция поддерживает заголовок, сетка — 1–6 колонок. Проверка всего дерева:
  белый список видов, глубина 16, узлы 256, суммарный текст 65536 символов,
  запрет циклов; ошибка ERR_WEB_TREE без частичного результата.
- Корни определяются по зависимостям children: дочерний UI не дублируется
  снаружи. Общие дети допускаются, циклы графа диагностируются. Свежесть
  результата после правок/F5 защищена прежним ключом 09B1.
- 26 новых unit/integration + 4 E2E; всего **534/534 + 62/62**, типы/сборка
  зелёные. Все 12 уроков, 09A/B1 и панели 08B сохранены. Добавлена практика
  сборки в WEB.md; в библиотеке теперь 89 доступных деталей из 165 определений.
- Формат v1, хранилище и транспорт не менялись. Изображения/ссылки/поля/окна
  и полный Web-конструктор остаются в плане. Деплой не выполнялся.

## 0.5.39 — 2026-09-14 — Текст и заголовок Web

- Закрыт 09B1: существующие web.text/web.heading доступны в библиотеке,
  имеют настройки, runtime и безопасное отображение в превью (p/h1–h6).
- Входы приоритетнее настроек; пустой текст допустим, неверный уровень
  1–6/тип данных даёт понятную ошибку. HTML отображается как текст.
- Элементы с проводами используют только успешный актуальный результат.
  Правки схемы скрывают устаревший текст; перемещение узлов его не сбрасывает.
  После F5 нужен новый запуск. Снимок захватывается до async-выполнения.
- 15 новых unit/integration и 3 браузерных сценария; всего **508/508 + 58/58**.
  Все 12 уроков, 09A и адаптивные панели сохранены; типы/сборка зелёные.
- Формат проекта v1, хранилище и Telegram-транспорт не менялись. Контейнеры
  и прочие Web-элементы остаются в плане. Деплой не выполнялся.

## 0.5.38 — 2026-09-14 — Адресные события Web-превью

- По выбору владельца начат отдельный план 09 «Telegram и Web». 09A
  завершён: нажатие кнопки/отправка формы запускает только соответствующий
  экземпляр и его цепочку, даже при одинаковых подписях.
- Необязательный `TriggerPayload.targetNodeId` адресует точку входа
  активного холста. Тип события/источник по-прежнему проверяются; неверный
  адрес не превращается в массовый запуск. Без адреса поведение сохранено.
- Загрузка/перезагрузка превью адресует первую показанную страницу.
  Маркер загрузки учитывает проект, уровень модели и ID страницы;
  черновики форм сбрасываются при смене контекста.
- Формы принимают только JSON-объект: неверный JSON, null, массив и
  примитив показывают ошибку и блокируют отправку. Ноль, false, вложенные
  поля и пустой объект сохраняются; ошибки не подменяются `{}`.
- Добавлены подписи полей и ошибки для доступности, aria-label перезагрузки;
  служебный адрес не добавляется в пользовательские выходные данные.
- 19 новых unit/integration + 3 E2E: изоляция цепочек, одинаковые подписи,
  страницы, формы, F5 и фактические значения в логе. **493/493** (63 файла),
  **55/55 E2E**, typecheck, строгие типы E2E и build успешны.
- Формат проекта v1, модели, уроки и старый симулятор сохранены. Полный
  рендеринг Web-страницы и расширение Telegram — следующие подэтапы.
  Деплой и серверное хранилище не выполнялись.

## 0.5.37 — 2026-09-14 — Панели редактора на узких экранах

- 08B завершён: при ширине меньше 1024 px доступны кнопки «Детали»,
  «Свойства», «Холст». Открыта максимум одна боковая панель, по умолчанию
  обе закрыты. Скрытые поля не получают Tab; поиск сохраняется при закрытии.
- Открытие переводит фокус в поле панели, Escape и крестик инспектора
  возвращают фокус на переключатель. Escape не прерывает урок; открытое
  меню получает его раньше панели. Панель не модальная — холст остаётся доступен.
- Вставка детали/фрагмента закрывает панель и рассчитывает свободную
  позицию по всему холсту. Старые позиции не меняются при resize.
- Ниже 1280 px дополнительные инструменты раскрываются отдельной кнопкой;
  запуск, сохранение и отладка остаются в тулбаре. На узком экране отладка
  изначально закрыта, её вкладки прокручиваются горизонтально, высота
  содержимого ограничена. Миникарта скрыта, масштаб и вписывание сохранены.
- Подсветка урока у скрытой панели указывает на её переключатель.
  Неперемещённое окно шага на узком Canvas располагается снизу;
  ручное положение, перенос и исправления 0.5.36 сохраняются.
- 3 новых unit + 3 E2E: 390/768 px, вставка/настройка/запуск, фокус,
  Escape/крестик, инструменты, отладка, F5, возврат к 1440 px и полный
  урок знакомства с Canvas на 390 px. Все 12 настольных уроков также зелёные.
- Всего **474/474 unit/integration (61 файл), 52/52 E2E**; typecheck,
  строгие типы E2E и build успешны. План 08 (A–B и поправки) закрыт.
- Это адаптация панелей, не заявление о полном сенсорном редакторе.
  Формат проекта, прогресс и зависимости не менялись. VPS не обновлялся;
  последняя показанная владельцем серверная рабочая копия — 0.5.28.

## 0.5.36 — 2026-09-14 — Поправки уроков по скриншоту владельца

- Окно урока и завершения можно перемещать за заголовок. Стрелки двигают
  его на 16 px, Shift — на 48 px, Home/кнопка ↺ возвращают исходную позицию.
  Положение сохраняется в sessionStorage вкладки между шагами и после F5;
  при изменении размера окна/карточки оно ограничивается экраном.
- Во время протягивания провода карточка пропускает события к портам под
  ней: наложение подсказки больше не превращает правильное соединение
  в промах с открытием быстрой вставки. Затемнение уменьшено с 70% до 35%.
- «Проверить шаг» больше не молчит при невыполненном условии. Для соединения
  показывает точные требуемые детали и порты, объясняет, что другой выход
  или промежуточная деталь не выполняют задание. Проверки не ослаблены.
- Escape сначала закрывает быструю вставку/контекстное меню, не прерывая
  урок. Новый жест соединения закрывает предыдущее меню.
- Регрессия всех 12 полных уроков + 14 новых E2E: сценарий скриншота
  (1280 px, масштаб, неверный user_id, лишняя security.mask, перекрытый
  вход, исправление, реальное значение в логе, F5 и завершение), клавиатура,
  resize и отдельная проверка окна каждого урока. 12 новых integration
  проверяют исправление неверного порта во всех connect-шагах каталога;
  ещё 5 unit — геометрию окна.
- Итого **471/471 unit/integration (61 файл), 49/49 E2E**; typecheck,
  строгие типы E2E и build успешны. Формат проекта и прогресса не менялись.
- Это внеплановые поправки перед 08B; панели узких экранов ещё впереди.
  В этой сессии сайт владельца не развёртывался/не обновлялся.

## 0.5.35 — 2026-09-14 — Копирование фрагментов без наложений

- 08A завершён: Ctrl/Cmd+D и Ctrl/Cmd+C/V переносят копию фрагмента
  единым смещением на свободное место; внутренние позиции и связи
  сохраняются, старые узлы не переставляются. Больше нет смещения +36/+36
  с наложением и случайной точки вставки с неверным экранным смещением.
- Новый фрагмент выделяется; поиск учитывает реальные размеры, масштаб,
  библиотеку, инспектор и отладку. При переполнении камера показывает
  фрагмент справа от схемы (минимальный масштаб остаётся 0.15).
- Общая геометрия перенесена в `src/lib/node-placement.ts`; клонирование
  в сторе объединено. Буфер — независимый снимок узлов и внутренних рёбер,
  новые ID и настройки не разделяются с исходником, один шаг undo/redo.
- Контекстное меню сохраняет множественное выделение при правом клике
  по его участнику; по невыделенной детали — выбирает именно её.
  Клавиатурные сочетания в полях ввода не перехватываются.
- 13 новых unit/integration и 4 E2E: относительные/старые координаты,
  рабочие связи, повторная вставка, настройки, undo/redo/F5, видимость
  между панелями и правильная цель контекстного меню.
- Проверки: **454/454** (59 файлов), **35/35** браузерных E2E; typecheck,
  типы E2E и build успешны, регрессия всех 12 уроков пройдена.
- Формат v1 не меняется: дробные позиции по-прежнему округляются при
  сохранении. Рамки групп и определения моделей не клонируются. Старые
  наложения внутри фрагмента сохраняются вместе с его раскладкой.
- Владелец выбрал дальнейший Canvas UX: отдельный план 08, следующий
  подэтап 08B — панели на узких экранах. План 07 остаётся закрытым.
  Серверное хранилище и продакшн-деплой не выполнялись.

## 0.5.34 — 2026-09-14 — Вставка деталей без наложений

- 07C завершён: библиотека (клик/drag & drop), стартовая и быстрая вставка
  от порта используют общий поиск свободного места. Ранее несколько
  деталей появлялись друг на друге в центре.
- Учитываются измеренные размеры, оценка по числу портов до первого
  измерения, масштаб, библиотека, инспектор и высота области над отладкой.
  Зазор — 24 единицы холста, позиции целые для стабильного сохранения v1.
- На заполненном экране новая деталь появляется справа от схемы; камера
  показывает её без изменения координат старых узлов. При необходимости
  масштаб уменьшается в существующих пределах редактора.
- Исправлена передача экранных координат для drop и быстрой вставки:
  смещение Canvas больше не вычитается повторно. Автосоединение сохранено.
- Чистая геометрия в `node-placement.ts`: 11 unit-тестов. Четыре E2E
  проверяют старые позиции, разные высоты, заполненный экран/масштаб,
  undo/redo/F5, настоящие перетаскивания и рабочую связь быстрой вставки.
- Проверки: **441/441** unit/integration (58 файлов), **31/31** E2E,
  typecheck, типы E2E и build успешны. Все 12 уроков продолжают проходиться.
- Формат проекта, прогресс, размеры компактных блоков не менялись.
  Старые наложения, ручной drag, дублирование и вставка буфера не перераскладываются.
  План 07 (A–C) завершён; продакшн-деплой не выполнялся.

## 0.5.33 — 2026-09-14 — Все 12 уроков пройдены через браузер

- 07B4 завершён, весь 07B закрыт: полные отдельные браузерные сценарии
  теперь есть для каждого из 12 уроков. Пять новых E2E проходят знакомство,
  порты, первую цепочку (включая отрицательный сценарий) и публикацию.
- Первая цепочка больше не засчитывает успех без передачи сообщения в лог
  и не пропускает просмотр журнала. Используются существующие проверки
  `expectedOutputs`, `open-debug.tab` и точные порты обоих концов связи.
- Уточнены инструкция запуска и название входа «ID чата». Компактная
  раскладка, формат v1, ID/порядок/число шагов и прогресс сохранены.
- Урок публикации подтверждён как теория: не создаёт учебный проект,
  не меняет сохранённую рабочую схему и не отправляет запросов записи API.
- E2E-помощник `openLesson` задаёт только явно переданные предпосылки
  и запрещает включать целевой урок в завершённые фикстуры.
- Проверки: **430/430** unit/integration (57 файлов), **27/27** E2E,
  typecheck, типы E2E и build успешны. Предупреждение о JS-фрагменте >500 kB
  остаётся. Продакшн не разворачивался; далее 07C — вставка без наложений.

## 0.5.32 — 2026-09-14 — Веб, модели и отладка: практика без ложного успеха

- 07B3: пять новых браузерных сценариев — веб-превью, создание/исполнение
  модели и F5, журнал/порты/история, ошибка внутри модели и исправление,
  автоматическая проверка после 30 записей истории.
- Явные `page_load`, `button_click`, `form_submit` запускают только
  соответствующий тип веб-триггера. Загрузка страницы больше не имитирует
  нажатие кнопки. Общий запуск без `event` сохранён для старых схем.
- Неуспех вложенной схемы передаётся наружу через «Вызов модели» вместо
  успешного пустого результата. Корректный пустой контракт по-прежнему
  возвращает `{}`. Внутренние журналы и побочные эффекты не откатываются.
- Урок создания модели требует ссылку на существующую модель проекта.
  Запуски трёх уроков требуют реальные выходы; соединения — точные порты.
- Просмотр журнала, портов и истории теперь отдельные проверяемые действия,
  без автопереключения. Наблюдатель реагирует на ID последнего запуска,
  а не длину ограниченной истории. Добавлены маркеры журнала/локальной истории.
- Исправлена инструкция выделения: Ctrl, на Mac — Cmd, не Shift.
  Объяснены пустой контракт, JSON веб-события и временная локальная история.
- Проверки: **426/426** unit/integration (56 файлов), **22/22** E2E,
  typecheck, типы E2E и build успешны; прежнее предупреждение >500 kB сохранено.
  Прогресс, ID/порядок/число шагов, компактная раскладка и формат v1 сохранены.
- 07B ещё не закрыт: осталось полное браузерное прохождение четырёх
  вводных/базовых/теоретических уроков (07B4), затем вставка без наложений.
  Продакшн-деплой не выполнялся.

## 0.5.31 — 2026-09-14 — Условия и команды: проверка реального результата

- 07B2: три браузерных сценария — обе ветки условия, игнорирование /help,
  ответ /start в заданный чат, неверная настройка команды, исправление и F5.
- Исправлен ложный зачёт условия по общему статусу схемы. Необязательные
  `expectedOutputs` в шагах запуска требуют выходы успешных деталей;
  условиям нужны `true` и лог, командам — `/start` и отправка сообщения.
- Результаты привязаны к ID последнего исполнения; не используются при
  запуске/сбросе. Источник и текст фиксируются при запуске, изменение формы
  или конфигурации не подменяет уже полученный результат.
- Урок команды ждёт вкладку чата. Уточнены русские инструкции: «Да»/«Нет»,
  поле «Сравнивать с», регистр сравнения, отдельное поле команды симулятора.
  Готовое значение /start в настройках принимается без лишнего действия.
- Общие E2E-помощники вынесены в `lesson-helpers.ts`; ошибочные связи
  отменяются через UI перед правильным соединением.
- Проверки: **412/412** unit/integration (54 файла), **17/17** E2E,
  `typecheck`, проверка типов E2E и `build` успешны. Известное предупреждение
  о размере JS-фрагмента сохранено. 07B ещё в работе: далее веб, модели, отладка.
- ID/порядок/число шагов, прогресс, компактная раскладка и формат проекта v1
  не менялись. Продакшн-деплой не выполнялся.

## 0.5.30 — 2026-09-14 — Практические уроки: результаты, вкладки и браузерный QA

- 07B1: полное прохождение конвертеров и эхо-бота через UI. Три новых E2E
  проверяют типы данных, точный текст/чат ответа, неверный порт/настройку,
  ошибку выполнения и исправление, F5 схемы и сохранение завершения урока.
- Исправлен пропуск просмотра результата: `open-debug.tab` требует
  конкретную вкладку открытой панели. Наблюдатель не переключает её за
  ученика. Старые шаги без `tab` сохраняют прежнее поведение.
- В снимке есть `debugTab`, в Completion Bridge — `DEBUG_TAB_CHANGED`;
  общий контракт вкладок для стора, панели и движка; валидация неизвестных вкладок.
- Подсветка находит кнопки `tab-*`; при закрытой панели показывает кнопку 🐞.
- Конвертеры требуют конкретные порты, инструкции двух уроков уточнены.
  Компактная раскладка, ID/число шагов, прогресс и формат проекта не менялись.
- Проверки: **402/402** unit/integration, **14/14** E2E в Chromium,
  `typecheck` и `build` успешны. 07B ещё в работе, следующий — 07B2.

## 0.5.29 — 2026-09-14 — Компактные детали и учебные материалы

- Canvas: детали шириной 240 px, входы/выходы в двух колонках, строки 24 px;
  типы показаны глифами, полные подписи и типы — в подсказках. Каждый
  маркер привязан к своей строке, устранено наложение портов в центре.
- Автовписывание не увеличивает первую деталь выше 100%; ручной зум сохранён.
- Академия: компактная сетка, переработанные описания 12 уроков,
  короткие инструкции + раскрываемая теория (`detailsKey`). План урока
  раскрывается по шагам; кнопка запуска перенесена перед планом.
- Учебная карточка 304 px: сворачивание без остановки, ограничение высоты,
  прокрутка; на шаге про инспектор карточка не закрывает его на широком экране.
- Исправлен пропуск «Выберите деталь»: автовыделение снимается один раз
  при входе в этот шаг и только в учебной песочнице.
- Урок публикации соответствует факту: серверный API уже публикует версии,
  переключатель режима холста — нет. Рабочий проект в теории не изменяется.
- Исправлены старые E2E-фикстуры (prerequisites, очистка при F5, неверный
  поиск, сворачивание категорий, перекрытые детали). Добавлены 4 браузерных
  сценария компактного интерфейса, включая клавиатуру и 390×640.
- Проверки: **395/395** unit/integration, **11/11** E2E в Chromium 153.0.8010.0,
  `typecheck` и `build` успешны. Браузер установлен вне проекта; конфиг
  принимает необязательный `PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH`.
- Синхронизированы устаревшие статусы; продолжение — `07-COMPACT-LEARNING.md`.
  Формат проекта v1, ID/порядок шагов и существующий прогресс не менялись.

## [0.5.28] — 2026-09-14

### Добавлено

**Визуальный жизненный цикл соединения деталей**

- Исходный порт активируется при начале перетаскивания (кольцо +
  свечение); совместимые порты подсвечиваются, несовместимые
  приглушаются с тултипом «Несовместимый тип: Текст → Число».
- Наведение перетаскиваемого соединения на совместимый порт —
  состояние «готов к подключению» (усиленное кольцо); цель ищется
  через `elementFromPoint`, стор обновляется только при смене цели —
  без перерисовок холста на каждое движение мыши.
- Успешное соединение: короткая вспышка обоих портов и блоков
  (~650 мс, без постоянного свечения) + одноразовый импульс по новой
  линии (подтверждение соединения, не анимация выполнения).
- Режимы эффектов «полные / уменьшенные / выключены» (миграция со
  старого переключателя) + учёт `prefers-reduced-motion`; кнопка в
  тулбаре (`data-testid="effects-mode"`).
- Академия: шаг «соединить» подсвечивает требуемые порты; после
  соединения одновременно вспышка и продвижение шага урока.
- E2E: `e2e/connection.spec.ts` — 3 сценария (совместимое соединение,
  несовместимое, интеграция с уроком); новые `data-атрибуты`:
  `data-connection-state`, `data-port-compatible/active/ready/id/direction`,
  `project-kind-*`, `create-project`.

### Изменено

- Временные эффекты соединений вынесены в эфемерный
  `connection-fx-store` — они не сериализуются в Project JSON и не
  смешиваются с состоянием выполнения (RUNNING/SUCCESS/ERROR идут из
  движка исполнения).

### Тесты

393 автотеста: состояния портов, вспышка и её сброс, интеграция
`handleConnect`, разделение с исполнением, сериализация без
транзиентных полей.

## [0.5.27] — 2026-09-14

### Исправлено

**Критический QA Академии: починен шаг «Выберите деталь» (4/5 урока
«Знакомство с Canvas»)**

- `select-block` сравнивал тип детали (`core.text`) с экземпляром узла
  и никогда не совпадал — урок зависал; теперь сверяется тип
  определения детали.
- `connect`: требуемые порты обязаны совпадать точно; соединение не тем
  портом или без портов шаг не завершает.
- `run` и `send-simulator-message`: засчитывается только НОВОЕ
  выполнение после появления шага (`run.at >= stepStartedAt`).

### Добавлено

- Единый Completion Bridge (`src/academy/events.ts`): нормализованные
  события из дифа снимков состояния — `BLOCK_ADDED/SELECTED`,
  `CONNECTION_CREATED`, `BLOCK_CONFIG_CHANGED`, `EXECUTION_FINISHED`,
  `SIMULATOR_MESSAGE_SENT`, `MODEL_CREATED`, `DEBUG_OPENED`,
  `PAGE_OPENED`; журнал событий урока (`lastEvent`/`recentEvents`).
- Углублённый статический валидатор уроков (`validate-deep.ts`): порты,
  настройки, цели подсветки, маршруты, невозможные шаги без песочницы.
- Тестовый обход всех 12 уроков (`lesson-walkthrough.test.ts`):
  каждый шаг обязан быть выполнимым.
- Браузерный E2E критического пути (Playwright, `npm run test:e2e`,
  4 сценария по `data-testid`).
- Кнопка «Проверить шаг» (фолбэк автопроверки), экран завершения урока
  с «Следующий урок», режим диагностики `?academyDebug=1`.
- `data-testid` для стабильных селекторов: `tutorial-*`,
  `library-item-<id>`, `library-search`, `canvas-node-<blockId>`,
  `run-button`, `lesson-card-<id>`, `lesson-start`, `onboarding-skip`.
- `docs/ACADEMY_QA.md` — полный отчёт аудита и источники истины по
  типам шагов.

### Тесты

374 автотеста (было 328): регрессия найденного бага, 14 интеграционных
сценариев через реальные сторы, валидатор + манифест, обход 12 уроков.

## [0.5.26] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.11 — Академия и обучение, часть H: финальный аудит и закрытие подэтапа**

- Курс расширен до 12 уроков: добавлены уровень 7 «Почему схема не
  работает?» (панель отладки: журнал, порты, история) и уровень 8
  «Черновик, LIVE и публикация» — с честной пометкой о фактическом
  статусе серверных версий.
- Глоссарий терминов `/academy/glossary`: 16 определений на русском,
  вход из Академии.

### Исправлено

- Наблюдатель и оверлей урока подняты на уровень приложения: уроки
  без песочницы (знакомство, публикация) идут без учебного проекта;
  раньше они ошибочно создавали песочницу.
- Ссылки на связанные детали в карточке урока вели на несуществующий
  маршрут `/academy/blocks` — исправлено на `/academy/reference`.

### Итог подэтапа 5.11

Полный цикл обучения в продукте: Академия → урок → учебный холст →
добавление деталей → соединения → запуск → автоподтверждение шагов →
завершение урока → прогресс; справочник деталей из реестра с честными
статусами, поиск помощи, контекстные подсказки, «ошибки как обучение»,
глоссарий, онбординг. 328/328 тестов, `typecheck` и `build` без ошибок.

## [0.5.25] — 2026-09-14

## [0.5.25] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.11 — Академия и обучение, часть G: онбординг первого запуска**

- `src/features/academy/OnboardingModal.tsx`: на первом заходе в
  Дашборд — «Что хотите собрать?» (Телеграм-бот / Web-страница /
  Изучить с нуля); выбор ведёт на первый урок дорожки; «Пропустить»
  закрывает окно — обучение никогда не запирает пользователя.
- Выбор сохраняется в прогресс Академии (`onboarding.choice`) через
  ту же абстракцию хранилища (сейчас LocalStorage, готова к серверу).

## [0.5.24] — 2026-09-14

## [0.5.24] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.11 — Академия и обучение, часть F: поиск помощи и контекстная помощь**

- `src/academy/search.ts` — чистое ядро: индекс помощи по урокам и
  деталям (`buildHelpIndex`/`searchHelp`), контекстные подсказки
  (`contextHelp`), «ошибки как обучение» (`lessonForErrorCode`,
  `helpRouteForError`).
- Поиск помощи на странице Академии: находит уроки и детали по
  названию, описанию, id и ключевым словам; результаты ведут на урок
  или карточку справочника.
- Контекстная помощь на Canvas: панель «?» в тулбаре показывает
  Академию, справочник и подсказки по состоянию холста (пустой холст,
  схема не запускалась, ошибка выполнения, выбрана деталь).
- Ошибки как обучение: у записей `ERR_*` в журнале отладки — ссылка
  «Как исправить?» на соответствующий урок (типы данных → конвертеры,
  цикл/триггеры → цепочка, модели → урок моделей; неизвестные коды —
  в справочник).

### Тесты

- `src/academy/search.test.ts`: индекс, поиск (приоритеты, минимальная
  длина), контекстные подсказки, маршруты «ошибки как обучение» —
  только существующие уроки. 11 новых тестов (всего 328/328).


## [0.5.23] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.11 — Академия и обучение, часть E: справочник деталей**

- `src/academy/reference.ts` — справочник строится автоматически из
  живого реестра блоков (второго списка нет): статус каждой детали
  честный (`effectiveStatus`), сложность, категория, входы/выходы
  (вид и тип порта), связанные уроки; фильтры по категории,
  сложности, статусу и поиск по названию/описанию/id/ключевым
  словам — чистые функции.
- `src/features/academy/BlockReferencePage.tsx` — маршрут
  `/academy/reference`: фильтры, счётчик, раскрывающиеся карточки
  (нативный `details`), запланированные детали помечены явно
  (бейдж «Запланировано» + предупреждение), ссылки на уроки с
  этой деталью.
- Справка из блока: кнопка «?» у каждой детали в библиотеке и
  ссылка в инспекторе открывают карточку справочника
  (`?block=<id>`, карточка раскрывается сама).
- Вход «📚 Справочник деталей» на странице Академии.

### Тесты

- `src/academy/reference.test.ts`: полнота относительно реестра,
  честность статусов, фильтры (категория/статус/сложность), поиск,
  комбинация фильтров, перекрёстные ссылки на уроки. 10 новых
  тестов (всего 317/317).

## [0.5.22] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.11 — Академия и обучение, часть D: базовый курс**

- `src/academy/lessons/base-course.ts` — 10 интерактивных уроков как
  данные: знакомство, Canvas, порты (вход/выход, формы маркеров),
  первая цепочка с запуском, условия («содержит»), конвертеры
  («Текст → Число»), бот-эхо в симуляторе (токен не нужен), команды
  Телеграма (`/start`), первый Web (веб-события), своя модель
  («Создать модель из выделенного» + спуск внутрь).
- Все шаги проверяются автоматически: добавление и выбор деталей,
  соединения портов, настройки, запуск, источник запуска, викторины;
  ни одной кнопки «Я сделал» в проверяемых шагах.
- Содержание уроков — только через i18n-ключи `academy.lessons.*`
  (заголовки, тексты, подсказки, вопросы, варианты).
- `src/features/academy/snapshot.ts` — `effectiveRunSource`:
  фактический источник запуска определяется триггерами холста, как и
  в рантайме — шаги вида `run`/`send-simulator-message` честны для
  веб-схем.
- Каталог Академии самодостаточен: наполнение реестра блоков через
  `import '@/blocks'`, `catalogErrors()` валидирует курс относительно
  живого реестра.
- `docs/ACADEMY.md` — устройство движка, формат урока, 12 типов
  шагов и их проверки, прогресс, оверлей, порядок добавления урока.

### Тесты

- `src/academy/catalog.test.ts`: каталог валиден относительно живого
  реестра; минимум 10 уроков; уникальность уроков и шагов; все ссылки
  на блоки реальны; **все** тексты имеют i18n-ключи; у викторин есть
  правильный ответ; `lessonsForBlock` связывает деталь с уроками;
  `prerequisites` без ссылок вперёд. 10 новых тестов (всего 307/307).

### Честные статусы

- Урок «Первый Web-интерфейс» честно помечает элементы оформления
  страницы (текст, поля, окна) как запланированные (`plannedNote`).

## [0.5.21] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.11 — Академия и обучение, часть C: Tutorial Overlay**

- `src/store/tutorial-store.ts` — активный урок: текущий шаг,
  автопроверка движком, подсказки, запись источника/текста запуска.
- `src/features/academy/snapshot.ts` — снимок состояния из живых сторов
  (схема, соединения, выбор, последний запуск, outbox, маршрут).
- `src/features/academy/TutorialWatcher.tsx` — старт урока по
  `?lesson=` в учебном проекте, проверка шагов на каждое изменение,
  автооткрытие панели отладки/вкладок для соответствующих шагов.
- `src/features/academy/TutorialOverlay.tsx` — подсветка цели
  (`data-tutorial`) затемнением вокруг элемента без блокировки
  интерфейса, карточка шага (текст, викторина, «Понятно», подсказка
  по кнопке и автоматически через 25 с, «Прервать урок», экран
  «Урок завершён»); клавиатура (Esc), `prefers-reduced-motion`,
  `aria-live`.
- Цели подсветки размечены: библиотека, холст, инспектор, «Запустить»,
  панель отладки и вкладки симулятор/чат/история; вкладка панели
  отладки и её открытость подняты в сторы (исполнения и UI).

## [0.5.20] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.11 — Академия и обучение, часть B: интерфейс Академии**

- Страницы `/academy` (уровни 1–9, карточки уроков, прогресс, «Продолжить
  обучение») и `/academy/lesson/:id` (описание, шаги, связанные детали,
  запуск в учебном проекте, сброс урока) в общем Premium Dark стиле.
- Учебные проекты-песочницы: опциональное поле `meta.tutorial` в формате
  (без смены версии формата, старые проекты валидны), `createSandboxProject`,
  `resetSandboxCanvas`, `detachSandbox` в хранилище проекта.
- Дашборд: учебные песочницы показываются отдельным разделом (продолжить /
  сделать обычным проектом / удалить), обычные проекты их не видят;
  вход в Академию — из шапки Дашборда и из тулбара Canvas («?»).
- Каталог уроков `src/academy/catalog.ts` и стор прогресса
  `src/store/academy-store.ts` (интерфейс готов к серверному хранению).

## [0.5.19] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.11 — Академия и обучение, часть A: Lesson Engine**

- `src/academy/types.ts` — модель урока как данных: уровни 1–9, дорожки,
  12 типов шагов (чтение, переход, добавление детали, соединение,
  настройка, запуск, сообщение симулятора, выбор, модель, отладка,
  публикация, викторина), снимок состояния продукта, прогресс.
- `src/academy/completion.ts` — автоматическая проверка шагов по снимку
  (без «Я сделал»: состав схемы, соединения, запуск, ответы).
- `src/academy/validate.ts` — честность каталога: все ссылки на блоки
  обязаны существовать в реальном реестре; дубли, зависимости и циклы
  ловятся.
- `src/academy/progress.ts` — прогресс (начат/шаг/завершён, проценты
  урока/уровня/каталога, доступность по предшествующим, сброс урока);
  хранилище за интерфейсом `ProgressStorage` (сейчас LocalStorage,
  готово к серверному).
- 19 новых тестов движка (всего 297/297).

## [0.5.18] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.10 — локальный запуск, Windows и development UX (Этап 5 пройден полностью)**

- `start-nodezzle.bat` — запуск двойным кликом на Windows: переход в
  свою папку, проверки `node`/`npm`/`package.json`, установка
  зависимостей при первом запуске, все сообщения на русском, окно не
  закрывается при ошибке; секретов в файле нет.
- `scripts/dev-all.mjs` (`npm run dev:all`) — единая команда запуска:
  сервер АПИ (`127.0.0.1:4210`) + интерфейс (`5173`, при занятости
  порта Vite берёт следующий свободный), процессы стартуют напрямую
  через `node` без process-manager'ов; health-проверка
  `/api/health`; автооткрытие браузера; русские сообщения об ошибках
  (занятый порт, неверная конфигурация, отсутствие зависимостей).
- Корректная остановка: `Ctrl+C` завершает оба процесса; висящих
  дочерних процессов не остаётся (проверено фактически).
- `scripts/dev-reset.mjs` (`npm run dev:reset`) — сброс только
  локальных данных (`server/data/`), всегда с подтверждением
  (`--yes` — без вопроса); автоматически не запускается.
- 9 тестов помощников лаунчера (разбор вывода, адреса, классификация
  ошибок, открытие браузера); всего 278/278 тестов.
- Документация: `docs/LOCAL_RUN.md` (установка, порты, локальная БД,
  остановка, обновление, backup, сброс, типовые ошибки), раздел
  «Быстрый запуск на Windows» в README; `.env.example` уточнён
  (токен Телеграма для локального запуска не нужен; секреты сессии в
  разработке генерируются автоматически).
- Проверка на чистой копии репозитория без `node_modules`: установка →
  лаунчер → сервер и интерфейс → health — всё работает.

## [0.5.17] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.9 — журналы и аудит, часть E: АПИ и русский интерфейс (подэтап 5.9 закрыт)**

- АПИ истории исполнений (`server/src/routes/history.ts`):
  - `GET /api/projects/:id/executions` — список (новые → старые,
    фильтр по статусу, пагинация: по умолчанию 25, максимум 100;
    неверные параметры — 400; неизвестный статус — 400);
  - `GET /api/executions/:executionId` и `…/steps` — исполнение и
    порядок шагов (узел, тип блока, статус, тайминги, сводки
    входа/выхода, код ошибки);
  - `GET /api/workspaces/:id/audit` — журнал действий: пагинация,
    фильтр по действию (точное) или группе (`auth.` = префикс),
    фильтр по типу цели; метаданные возвращаются распарсенными.
- Изоляция пространств на бэкенде: чужое исполнение, чужие шаги и
  чужой журнал отдают 404 без утечки данных; без сессии — 401.
- Журнал только на чтение: запись создаёт исключительно сервер;
  `POST/PUT/DELETE` по пути журнала — 405.
- Фронтенд на русском: вкладка «История» в панели отладки показывает
  серверную историю (статус, источник, дата, длительность, число
  шагов, ошибка; раскрытие — детали шагов) и отдельную локальную
  историю симулятора; на Дашборде — раздел «Журнал действий»
  (дата, пользователь, действие, объект, безопасные метаданные,
  фильтр по группам, «Показать ещё»).
- Новые методы АПИ-клиента (`executions`, `execution`,
  `executionSteps`, `audit`) и форматтеры даты/длительности (ru-RU).
- 7 новых тестов (всего 269/269): АПИ истории/аудита (пагинация,
  лимиты, неверные параметры, сортировка, фильтры, изоляция,
  только чтение) и клиентские методы.

## [0.5.16] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.9 — журналы и аудит, часть D: журнал действий**

- Хранилище `server/src/audit/store.ts`: только добавление — кода
  изменения/удаления записей нет; метаданные всегда проходят
  санитайзер.
- События пишут серверные маршруты (пользовательского АПИ записи нет):
  `auth.register`, `auth.login`, `auth.logout`;
  `project.create`, `project.update`, `project.delete`;
  `version.create`, `version.restore`; `project.publish`;
  `secret.create`, `secret.delete`;
  `telegram.bot_connect`, `telegram.bot_update`,
  `telegram.bot_disconnect`, `telegram.webhook_register`,
  `telegram.webhook_delete`.
- Актор — фактический пользователь сессии; системный актор — только
  явный (`NULL`); подделка пользователя невозможна.
- В журнал не попадают значения секретов, токены и вебхук-путь
  (проверено тестами).
- Раздел «Журнал действий» в `docs/SECURITY.md` (§3.5).
- 6 новых тестов (всего 262/262): события аккаунта, жизненный цикл
  проекта, секреты/боты/вебхук, только добавление, санитайзер,
  разделение акторов.

## [0.5.15] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.9 — журналы и аудит, часть B: интеграция с рантаймом**

- Хранилища `server/src/execution/journal.ts`: исполнения
  (`старт → завершение`, статусы, тайминги) и шаги (авто-нумерация,
  лимит 500 шагов на исполнение).
- Запуск с журналированием `server/src/execution/record.ts`:
  - ручной запуск (`/api/projects/:id/execute`) — `trigger_type='api'`;
  - обновление Telegram через вебхук — `trigger_type='telegram'`,
    бот и `update_id` фиксируются в исполнении;
  - статусы `success|error|stopped|timeout`, код ошибки и
    продолжительность; шаги — из телеметрии узлов в порядке начала,
    сводки входов/выходов — только через санитайзер (5.9C).
- Черновик не исполняется: без публикации исполнений не создаётся
  (проверено тестом); правки черновика не влияют на журнал.
- 7 новых тестов (всего 256/256): хранилище, порядок шагов, лимит
  шагов, ручной запуск, таймаут, источник `telegram`, изоляция
  черновика, маскирование токена в сводках.

## [0.5.14] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.9 — журналы и аудит, часть C: централизованный санитайзер**

Санитайзер выполнен до интеграции с рантаймом: безопасная запись
сводок первична, поэтому в истории он идёт раньше части B.

- Модуль `server/src/security/sanitize.ts`: единая точка
  `sanitize`/`summarize` для журналов исполнений и аудита.
- Маскирование не полагается только на имя поля: чувствительные
  КЛЮЧИ (`token`, `password`, `authorization`, `cookie`, `secret`,
  `api_key`/`apiKey`, `database_url`, `webhook_secret`, `private_key`,
  `credential`…) маскируются независимо от значения; значения, похожие
  на секреты (токен бота `123456:…`, заголовки `Bearer`/`Basic`),
  маскируются независимо от ключа, включая вхождения внутри длинных
  строк (например, в сообщениях об ошибках).
- Лимиты: глубина 8, строка 400 символов, массив 40 элементов, объект
  64 ключа, сводка до 2 КБ — большой полезный груз не кладёт сервер и
  не раздувает БД.
- Безопасны: циклические ссылки, функции, символы, `undefined`, даты,
  `Error` (стек сознательно не сохраняется). Маркер маскирования —
  `[СКРЫТО]`; первые/последние символы реальных токенов не
  сохраняются.
- Раздел «Санитайзер журналов» в `docs/SECURITY.md` (§3.5).
- 13 новых тестов (всего 249/249): все обязательные сценарии —
  вложенные секреты, массивы, токен, заголовки, куки, пароль,
  API-ключ, `DATABASE_URL`, большой и глубокий JSON, циклы, ошибки.

## [0.5.13] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.9 — журналы исполнения и аудит, часть A: схема БД**

- Миграция `009_executions`: история выполнений — рабочее
  пространство, проект, версия, тип и источник триггера, бот,
  внешнее событие, родительское исполнение, статус
  (`queued|running|success|error|stopped|timeout` — контролируемый
  набор `CHECK`), код ошибки, тайминги; индексы под запросы истории.
- Миграция `010_execution_steps`: шаги исполнения — узел, тип блока,
  порядок, статус, тайминги, сводки входов/выходов (полные полезные
  нагрузки не хранятся); задел под будущий Time Travel Debug.
- Миграция `011_audit_logs`: журнал действий — рабочее пространство,
  актор (допустим системный), действие, цель, метаданные; только
  добавление на уровне приложения, пользовательского изменения нет.
- Раздел о будущем хранении журналов в `docs/DATABASE.md` (§2.4):
  политика «N дней» и лимиты — отдельным решением; у журнала действий
  собственная, более долгая политика. Автоматическое удаление сейчас
  выключено.
- 5 новых тестов схемы (всего 236/236).

## [0.5.12] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.8 — реальный Telegram, часть F: управление вебхуком. Подэтап 5.8 завершён**

- Маршруты управления вебхуком бота: `POST` (зарегистрировать), `GET`
  (проверить статус: фактический адрес в Telegram, очередь, ожидаемый
  путь) и `DELETE` (удалить) — `/api/workspaces/:id/telegram-bots/:botId/webhook`.
- Безопасные операции: регистрация выполняется только при публичном
  HTTPS-адресе без учётных данных; `http://`, `localhost`, локальные и
  служебные диапазоны отклоняются (400) ещё до обращения к Telegram.
  Реальный вебхук без реального токена и публичного адреса не
  регистрируется; локальная разработка работает без вебхука.
- Секретный путь вебхука передаётся в Telegram и как секретный
  заголовок (`secret_token`).
- Транспорт расширен: `setWebhook`, `getWebhookInfo`, `deleteWebhook`.
- 6 новых тестов (всего 231/231) — только мок-транспорт, реальный
  Telegram не вызывается.

### Итог подэтапа 5.8 (критерий готовности выполнен)

Доказанная тестами цепочка: **обновление Telegram → сервер → только
опубликованная (LIVE) версия → рантайм → действие → мок-транспорт**;
черновик не исполняется; токен нигде не раскрывается (только Secrets
Vault); симулятор продолжает работать без токена и без сервера.

## [0.5.11] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.8 — реальный Telegram, часть E: панель подключения**

- Панель «Telegram-бот» на Панели управления (русский интерфейс,
  `src/features/telegram/TelegramBotPanel.tsx`): подключение бота,
  замена токена, отключение, статус и имя/ник бота, привязка к
  проекту. Если сервер не запущен или вход не выполнен — панель
  показывает подсказку; симулятор схем продолжает работать без
  сервера.
- Токен вводится один раз в поле пароля и уходит только на сервер
  (в шифрованное хранилище); после подключения показываются лишь имя
  секрета, имя/ник бота и статус. Токен не сохраняется в браузере и
  не попадает в файл проекта.
- АПИ-клиент фронтенда `src/lib/server-api.ts`: относительные
  запросы (в разработке проксируются Vite на `127.0.0.1:4210`),
  сессия — в куке сервера, не в `localStorage`; ошибки — тип
  `ApiClientError` с кодом и деталями.
- Чистая модель панели `src/features/telegram/bot-connect-model.ts`
  (валидация формата токена и имени секрета до отправки, оркестрация
  «секрет → бот»).
- 11 новых тестов (всего 225/225): АПИ-клиент и модель подключения —
  без сети.

## [0.5.10] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.8 — реальный Telegram, часть D: LIVE-рантайм из вебхука**

- Обработчик `server/src/telegram/handler.ts` подключён к вебхуку по
  умолчанию: обновление → бот по секретному пути → токен из Secrets
  Vault → исполнение **только опубликованной (LIVE) версии** с общими
  лимитами рантайма → исходящие сообщения через транспорт.
- Черновик никогда не исполняется вебхуком (проверено тестом: правки
  черновика после публикации не влияют на ответы бота); бот без
  привязки или без публикации молчит; отсутствие секрета не роняет
  вебхук.
- Сквозной тест цепочки «обновление → сервер → LIVE → рантайм →
  действие → мок-транспорт» (5 тестов); реальный Telegram в тестах
  не вызывается.

### Изменено

- Миграция `008_telegram_bots` (до релиза Этапа 5): `secret_id`
  получил `ON DELETE CASCADE` — удаление секрета каскадно отключает
  бота, вебхук-путь перестаёт отвечать.

## [0.5.9] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.8 — реальный Telegram, часть C: транспорт Bot API**

- Модуль `server/src/telegram/api.ts` — изолированный серверный клиент
  Bot API: `getMe`, `sendMessage`, `sendPhoto`, `editMessageText`,
  `deleteMessage`, `answerCallbackQuery` (методы, которые уже
  востребованы реестром блоков и планом; остальные добавляются по мере
  появления блоков).
- Таймаут запроса (по умолчанию 10 с, `AbortSignal`), ошибки Telegram
  и сети нормализуются в `TelegramApiError` с описанием от Telegram.
- Безопасность: токен знает только транспорт (берёт расшифрованное
  значение из Secrets Vault), не пишет его в ошибки и журнал; базовый
  адрес фиксирован (официальный домен либо локальный стенд) —
  пользовательские адреса отклоняются (защита от SSRF).
- 8 новых тестов (всего 209/209) — только на мок-транспорте: реальные
  запросы к Telegram в тестах запрещены.

## [0.5.8] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.8 — реальный Telegram, часть B: вебхук-слой**

- Эндпоинт `POST /api/telegram/webhook/:path` (без сессий — его
  вызывает Telegram): путь вебхука — случайные 32 байта; ограничение
  частоты по ключу «адрес + путь» (120/мин по умолчанию, подменяется
  в тестах); общий лимит размера тела.
- Строгая валидация обновления схемой (подмножество `message` /
  `edited_message`): недоверенные данные отклоняются (400), в событие
  попадают только описанные поля.
- Нормализация в событие NODEZZLE: `TriggerPayload` с текстом,
  пользователем и чатом; команды `/start`, `/help@Бот аргументы`
  распознаются в поле `command`.
- Ошибка обработки обновления не ломает ответ — нет предлога для
  повторных доставок от Telegram.
- Тело обновления и токены не пишутся в журнал.
- 12 новых тестов (всего 201/201), все — на фиктивных данных, без
  реальной сети и токенов.

## [0.5.7] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.8 — реальный Telegram, часть A: конфигурация ботов**

- Миграция `008_telegram_bots`: бот привязывается к рабочему
  пространству и (опционально) к проекту; токен хранится **только**
  ссылкой на секрет (`secret_id`) — значения в таблице нет; публичный
  путь вебхука — случайные 32 байта (`webhook_path`); один бот на
  проект (`UNIQUE`).
- Маршруты пространства: `POST/GET /api/workspaces/:id/telegram-bots`,
  `GET/PATCH/DELETE /api/workspaces/:id/telegram-bots/:botId`
  (просмотр, замена секрета/проекта, отключение).
- Безопасность: токен не возвращается ни в одном ответе (проверено
  тестами); чужой `secret_id` использовать нельзя — 404 без раскрытия
  существования; проект и пространство проверяются сервером; без
  сессии — 401.
- Хранилище секретов получило чтение метаданных без значения
  (`SecretStore.meta`) — связывание бота с секретом не касается
  шифротекста.
- Маршрутизатор сервера поддерживает метод `PATCH`.
- 5 новых тестов (всего 189/189). Реальный токен для разработки и
  тестов не нужен; симулятор продолжает работать без токена.

## [0.5.6] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.7 — серверный рантайм с лимитами**

- Маршрут `POST /api/projects/:id/execute`: исполнение схемы на
  сервере через общий движок (`executeCanvas`) и общий реестр блоков.
  Рантайм использует **только опубликованную (LIVE) версию** —
  черновик не исполняется; правки черновика после публикации на
  исполнение не влияют.
- Лимиты исполнения: число шагов и повторов узла — внутренние
  константы движка; таймаут — `NODEZZLE_EXEC_TIMEOUT_MS` (по умолчанию
  10 с, исполнение отменяется, ответ `{"result":{"status":"timeout"}}`);
  параллельные исполнения — `NODEZZLE_EXEC_PARALLEL` (по умолчанию 2,
  превышение — `429`); размер полезной нагрузки — общий лимит тела
  запроса.
- Валидация публикации дополнена статическими проверками моделей:
  `MODEL_DEPTH_EXCEEDED` (вложенность > 5) и `MODEL_CYCLE`
  (циклический вызов моделей).
- 10 новых тестов (всего 184/184): исполнение с исходящими
  сообщениями, неизменяемость публикации, таймаут, параллельный отказ,
  400/401/404, глубина и цикл моделей.

## [0.5.5] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.6 — публикация: серверная валидация → неизменяемая LIVE-версия**

- Сервер исполняет общий с клиентом реестр блоков (один источник
  правды): загрузчик-хук `server/src/alias-loader.mjs` разрешает
  алиас `@/` и безрасширительные импорты фронтенда; запуск —
  `npm run server:dev` (с `--import ./server/src/register-alias.mjs`).
- Модуль `server/src/publication/validate.ts` (проверки перед
  публикацией): пустая схема; дубли узлов; отсутствующие в реестре и
  нереализованные (запланированные) блоки; концы и порты рёбер;
  обязательные входы без подключения; циклы (рекурсия); ссылки на
  несуществующие модели; отсутствие стартовой точки.
- Маршруты: `POST /api/projects/:id/publish` (невалидная схема —
  `422 VALIDATION_FAILED` со списком проблем) и
  `GET /api/projects/:id/live`. Одна публикация на проект: прежняя
  LIVE автоматически уходит в `ARCHIVED`.
- Миграция `007_version_status`: статусы версий
  `SNAPSHOT` / `LIVE` / `ARCHIVED`.
- Ошибки АПИ получили необязательное поле `details`
  (машинная информация, например список проблем валидации).
- 12 новых тестов (всего 176/176): чистая валидация и публикация
  через АПИ (включая архивацию прежней публикации).

### Изменено

- Ядро фронтенда приведено к «стираемому» TS-синтаксису без
  parameter properties (`ExecutionAbort`, `ProjectStorage`) — код
  исполняется и в сборке, и нативным Node (нужно для общего с сервером
  реестра и будущего серверного рантайма).

## [0.5.4] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.5 — Secrets Vault**

- Миграция `006_secrets`: шифротекст + IV + тег аутентификации,
  уникальность имени секрета в пространстве.
- Модуль `server/src/security/vault.ts`: AES-256-GCM через
  `node:crypto`; ключ — `NODEZZLE_VAULT_KEY` (32 байта hex), при
  отсутствии — детерминированный вывод из секрета сессий (локальный
  запуск «из коробки»).
- Маршруты пространства: `POST/GET /api/workspaces/:id/secrets`,
  `DELETE /api/workspaces/:id/secrets/:secretId`. АПИ отдаёт только
  метаданные (имя, идентификатор, даты); значения не возвращаются
  клиенту и расшифровываются только внутри сервера для рантайма.
- 10 новых тестов (всего 167/167): крипто-модуль (ключи, шифрование,
  неверный ключ) и маршруты (значения не утекают в ответы, дубли,
  изоляция, удаление).
- `.env.example`: переменная `NODEZZLE_VAULT_KEY`.

## [0.5.3] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.4 — версии проектов (черновик и снапшоты)**

- Миграция `005_project_versions`: снапшот — полная копия документа
  проекта на момент создания.
- Маршруты: `POST /api/projects/:id/versions` (создать снапшот с
  меткой; по умолчанию — «Версия от …» на русском),
  `GET /api/projects/:id/versions` (список),
  `GET /api/projects/:id/versions/:versionId` (просмотр копии),
  `POST /api/projects/:id/versions/:versionId/restore` (вернуть
  черновик к снапшоту).
- Семантика зафиксирована: документ в `projects` — редактируемый
  черновик (Draft); неизменяемые публикации (LIVE) появятся в
  подэтапе 5.6.
- 4 новых теста (всего 157/157): жизненный цикл версии, независимость
  снапшота от правок черновика, изоляция пользователей.

## [0.5.2] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.3 — рабочие пространства и проекты на сервере**

- Миграции `003_workspaces` (пространства + участники, роли) и
  `004_projects` (документ проекта целиком в колонке, колонки-индексы
  для списков).
- При регистрации автоматически создаётся пространство «Мои проекты».
- Маршруты: `GET/POST /api/workspaces`,
  `GET /api/workspaces/:id/projects`, `POST /api/projects`,
  `GET/PUT/DELETE /api/projects/:id`.
- Документ проекта валидируется тем же форматом
  `nodezzleProjectSchema` — клиент и сервер понимают один формат
  (импорт общего Zod из `src/core/project/schema.ts`).
- Безопасность по назначению: право доступа проверяет сервер по
  членству в пространстве (разрешениям клиента не доверяем); чужие
  проекты и пространства отвечают 404; идентификатор документа при
  обновлении должен совпадать с адресом.
- 7 новых тестов (всего 153/153): полный CRUD, дубли, невалидный
  документ, изоляция пользователей, 401 без сессии.

## [0.5.1] — 2026-09-14

### Добавлено

**Этап 5, подэтап 5.2 — аккаунты и сессии**

- Маршруты аккаунтов: `POST /api/auth/register` (сразу входит),
  `POST /api/auth/login`, `POST /api/auth/logout`, `GET /api/auth/me`.
- Пароли — только scrypt-хеши с солью (`серверная` проверка за
  постоянное время); формат хеша самодостаточный
  (`scrypt$N$r$p$salt$hash`).
- Сессии: случайный токен (32 байта); в базе хранится только его
  SHA-256 дайджест (миграция `002_sessions`); cookie — `HttpOnly`,
  `SameSite=Lax`, подпись HMAC-SHA256; в продакшне
  `NODEZZLE_SESSION_SECRET` обязателен (≥ 32 символов), в разработке —
  случайный.
- Ограничитель частоты входа/регистрации: 20 попыток / 10 минут с
  одного адреса (429).
- Ошибка входа единая — «Неверный адрес или пароль» (не раскрывает
  зарегистрированные адреса).
- 19 новых тестов (всего 146/146): хеши/сессионные подписи, подделки,
  истечение окна лимитов, полный сценарий регистрации/входа/выхода.

## [0.5.0] — 2026-09-14

### Добавлено

**Этап 5 (продуктовый), подэтап 5.1 — каркас сервера**

- Новый каталог `server/` — серверный фундамент **без новых рантайм-
  зависимостей**: `node:http` (API), `node:sqlite` (файловая БД),
  встроенный TypeScript type-stripping (Node ≥ 22.18). Добавлен только
  `@types/node` (devDependency, типы).
- Конфигурация окружения `server/src/config.ts`: хост/порт
  (`127.0.0.1:4210` по умолчанию), путь к БД, окружение, лимит тела
  запроса. Порты **3000 и 3001 отвергаются** всегда (чужой сайт на VPS).
- Централизованные ошибки `ApiError`: клиент всегда получает единый
  формат `{ "error": { "code", "message" } }` и корректные статусы
  (400/401/403/404/405/409/413/415/429/500).
- Мини-роутер с параметрами пути (`/api/.../:id`) и различением
  «не найдено» / «метод не разрешён».
- Слой БД: открытие/создание файла, `PRAGMA WAL`, мигратор —
  нумерованные идемпотентные `.sql`-файлы только вперёд, учёт в
  `schema_migrations`, транзакция на миграцию. Первая миграция —
  `001_users.sql` (таблица `users`).
- `GET /api/health` — состояние сервиса (статус, версия, окружение).
- 17 новых тестов (всего 127/127), включая **дымовой запуск реального
  процесса** `node server/src/index.ts` (ловит несовместимость со
  встроенным type-stripping).
- `.env.example` и `.gitignore` обновлены под сервер (`server/data/`).

### Изменено

- `npm run typecheck` проверяет и сервер (`tsc -p server/tsconfig.json`);
  новый скрипт `npm run server:dev` (`node --watch server/src/index.ts`).
- Витест включает тесты `server/src/**`.

## [0.4.0] — 2026-09-14

### Добавлено

**Этап 4 — Большая библиотека деталей: ЗАВЕРШЁН (часть H — финальный аудит)**

- Автотест каталога подтверждает порог Этапа 4: **16 обязательных
  категорий по 10+ определений, всего 165 (≥ 160)**; у каждой детали —
  русское имя и ключевые слова (включая скрытые заготовки).
- Зарезервированным деталям добавлены `status: 'planned'` и ключевые
  слова (единообразие каталога); запись стикера в `ru.json` переведена
  на плоский ключ (единый контракт имён).

### Итог Этапа 4 (части A–H)

- В реестре 165 определений вместо 33: 84 доступны в палитре
  (все с исполняемым кодом), 80 запланированы (контракты зафиксированы,
  скрыты из палитры — за реализованные не выдаются), 1 служебный стикер.
- Категорий 18; обязательные 16: ядро, логика, данные, поток,
  телеграм-события, телеграм-действия, веб-интерфейс, веб-события,
  модели, память, HTTP, дата/время, файлы/медиа, безопасность, отладка,
  конвертеры (+ AI и заметки).
- Механизмы: поиск и быстрая вставка работают для новых деталей через
  общий реестр и систему типов без отдельных каталогов; вкладка
  «Основные» — по полю `difficulty`; статусы — `BlockStatus`
  + `effectiveStatus()`; организация — модули по категориям
  (`src/blocks/<категория>/*.ts`), реестр масштабируется на сотни деталей.
- Документы синхронизированы: `docs/BLOCK_CATALOG.md`,
  `docs/IMPLEMENTATION_MATRIX.md`, `docs/STATUS.md`,
  `docs/agent-plan/STATUS.md`.

## [0.3.7] — 2026-09-14

### Добавлено

**Этап 4 — Большая библиотека деталей, часть G: отладка и конвертеры**

- **+12 определений** (в реестре стало 165; все 16 обязательных категорий
  теперь содержат по 10+ определений):
  - `Отладка` +9: Осмотр значения, Утверждение, Трассировка, Замер времени,
    Счётчик — исполняемые; Точка останова, Снимок, Наблюдатель,
    Экспорт журнала — запланированы;
  - `Конвертеры` +3 (исполняемые): Текст в массив, Массив в текст,
    Логическое в текст (новые идентификаторы `converters.*`; унаследованные
    конвертеры сохраняют идентификаторы `data.*`).
- Подкатегории, новые ключи портов и ошибка `ERR_ASSERTION` в `ru.json`.

## [0.3.6] — 2026-09-14

### Добавлено

**Этап 4 — Большая библиотека деталей, часть F: файлы/медиа и безопасность**

- **+20 определений** (в реестре стало 153):
  - `Файлы и медиа` +10 (запланированы): картинки (загрузка, размер,
    свойства), файлы (свойства, чтение текста, Base64 туда и обратно,
    скачивание по адресу), сведения об аудио и видео — исполнение вместе
    с файловым хранилищем;
  - `Безопасность` +10: Маскировать, Хеш-сумма, Случайный ИД,
    Случайный токен, Проверить почту, Очистить от разметки — исполняемые
    (хеш — контрольная сумма, не криптография); Получить/Список секретов,
    Ограничитель частоты, Журнал аудита — запланированы до серверного
    хранилища секретов.
- Подкатегории, новые ключи портов и русские описания в `ru.json`.

## [0.3.5] — 2026-09-14

### Добавлено

**Этап 4 — Большая библиотека деталей, часть E: HTTP и дата/время**

- **+18 определений** (в реестре стало 133):
  - `Дата/время` +9: Формат даты, Сдвинуть дату, Разница дат,
    Метка времени, Дата из метки, Часть: дата, Часть: время, День недели —
    исполняемые; Расписание — запланировано (нужен планировщик запусков);
  - `HTTP` +9 (запланированы): получить/отправить запрос, скачать и
    загрузить файл, вебхук и ответ на него, таймаут, кодирование адреса,
    сборка адреса — исполнение появится с серверным запуском.
- Подкатегории дат (преобразование/арифметика/части/триггеры) и HTTP
  (запросы/файлы/триггеры/ответы/настройки/преобразование); новые ключи
  портов и русские описания в `ru.json`.

## [0.3.4] — 2026-09-14

### Добавлено

**Этап 4 — Большая библиотека деталей, часть D: модели и память**

- **+17 определений** (в реестре стало 115):
  - `Модели` +9: Подготовка входа, Извлечь из результата, Запасное
    значение, Проверка результата, Разбор ошибки модели — исполняемые;
    Метаданные вызова, Список моделей, Защита входа, Проверка схемы —
    запланированы;
  - `Память` +8 (запланированы): чтение/запись/проверка переменных,
    счётчики (инкремент/декремент), списки (добавление/длина), очистка —
    исполнение появится вместе с переменными проекта.
- Подкатегории моделей: вход, результат, контракт, ошибки, справочник;
  подкатегории памяти: переменные, счётчики, списки, служебные;
  новые ключи портов и русские описания в `ru.json`.

## [0.3.3] — 2026-09-14

### Добавлено

**Этап 4 — Большая библиотека деталей, часть C: веб-интерфейс и веб-события**

- **+17 определений** (в реестре стало 98):
  - `Веб-интерфейс` +10 (запланированы): Текст, Заголовок, Картинка,
    Поле ввода, Многострочное поле, Ссылка, Контейнер, Секция, Сетка,
    Модальное окно — контракты и место в библиотеке, рендеринг появится
    с развитием веб-превью;
  - `Веб-события` +7 (исполняемые триггеры, как существующие веб-детали):
    Изменение поля, Окно открыто/закрыто, Клик по ссылке, Прокрутка,
    Нажатие клавиши, Изменение размера — с учётом вида события
    (`payload.web.event`), если он указан.
- Подкатегории `Веб-интерфейса`: оформление, ввод, навигация, структура;
  новые ключи портов и русские описания в `ru.json`.

## [0.3.2] — 2026-09-14

### Добавлено

**Этап 4 — Большая библиотека деталей, часть B: Telegram (события и действия)**

- **+16 определений** (в реестре стало 81; `telegram_events` и
  `telegram_actions` — по 10):
  - события: Нажата кнопка, Сообщение изменено, Получено фото,
    Получено голосовое, Получен документ, Получена локация,
    Получен контакт, Инлайн-запрос;
  - действия: Отправить документ, Изменить сообщение, Удалить сообщение,
    Ответ на кнопку, Отправить локацию, Отправить контакт,
    Получить пользователя, Получить чат.
- Все 16 — `planned` (скрыты из палитры): контракты портов и место в
  библиотеке зафиксированы, исполнение появится вместе с реальным
  транспортом бота; за реализованные не выдаются.
- Новые подкатегории (`subcategory`): inline, сообщения, вложения,
  отправка, запросы; новые ключи портов в `ru.json`.

## [0.3.1] — 2026-09-14

### Добавлено

**Этап 4 (Промпт №4) — Большая библиотека деталей, часть A:
ядро, логика, данные, поток**

- **+32 определения** (в реестре стало 65):
  - `Ядро` +5: Логическое, Массив, Объект, Ссылка (исполняемые значения),
    Шаблон (запланирован);
  - `Логика` +8: Сравнение, И, ИЛИ, НЕ, ЛИБО, Пусто?, В диапазоне,
    Первое значение — все чистые, исполняемые;
  - `Данные` +10: операции с объектами (поле, установка, ключи,
    объединение) и массивами (элемент, добавление, длина, склейка, срез,
    сортировка) — все исполняемые;
  - `Поток` +9: Заслонка, Разветвитель, Объединитель (исполняемые);
    Стоп, Повтор, Антидребезг, Ограничитель частоты, Очередь, Интервал —
    запланированы (`available: false`, не выдаются за реализованные).
- Фундамент библиотеки (назад совместимые расширения `BlockDefinition`):
  - поле `status` — `planned`/`prototype`/`implemented`/`experimental`/
    `deprecated`, вывод через `effectiveStatus()` реестра;
  - опциональные `version` и `subcategory`;
  - поле `difficulty` проставлено всем деталям реестра (вкладка «Основные»).
- Автотест каталога `src/blocks/library-catalog.test.ts`: уникальность
  идентификаторов, порты и типы, статусы, сложность, русские названия,
  поиск и быстрая вставка новых деталей (8 тестов, всего 108/108).

### Изменено

- Категории деталей: `BLOCK_CATEGORIES` содержит 18 идентификаторов —
  вместо единых `telegram`/`web` введены `telegram_events`,
  `telegram_actions`, `web_ui`, `web_events`; добавлена `converters`.
- 7 конвертеров типов выделены из «Данных» в категорию `Конвертеры`
  (идентификаторы блоков не менялись — формат проектов не затронут).
- Телеграм-детали распределены по событиям/действиям, веб-триггеры —
  в «Веб-события»; определение источника исполнений переведено на префиксы
  (`telegram*`/`web*`).
- Обновлены `docs/BLOCK_CATALOG.md`, `docs/STATUS.md`,
  `docs/IMPLEMENTATION_MATRIX.md`, `docs/agent-plan/STATUS.md`.

## [0.3.0] — 2026-09-14

### Добавлено

**Этап 3 (Промпт №3) — Мастер-спецификация: полная проектная память**

Код не менялся; создана система спецификаций, отличающая факт от планов
(легенда статусов: СПЕЦИФИКАЦИЯ / ЗАПЛАНИРОВАНО / ПРОТОТИП / РЕАЛИЗОВАНО /
ПОКРЫТО ТЕСТАМИ / ПРОДАКШН-ГОТОВО):

- `docs/MASTER_SPEC.md` — главная спецификация: сущности (деталь, порт,
  соединение, холст, группа, модель, контракт, проект, триггер, выполнение),
  состояния, системные правила, ключевые сценарии, карта страниц.
- `docs/DESIGN_SYSTEM.md` — токены темы, семантика состояний, цвета/глифы
  типов портов, цвета категорий, компоненты, анимации, правила развития.
- `docs/SECURITY.md` — действующие правила секретов, модель угроз и
  контрмеры, целевая модель (аутентификация, хранилище секретов, изоляция,
  лимиты), чек-лист.
- `docs/DATABASE.md` — фактическое хранилище (ключи LocalStorage,
  валидация, автосохранение) и целевая серверная схема БД
  (пользователи, рабочие пространства, проекты, версии, выполнения, секреты).
- `docs/BLOCK_CATALOG.md` — полный каталог с фактическими статусами:
  33 определения (25 в палитре, из них 14 покрыты исполнением в тестах;
  7 зарезервированных; 1 служебный стикер) и соответствие целевым
  16 категориям Этапа 4.
- `docs/STATUS.md` — технический статус продукта (синхронизирован с
  `docs/agent-plan/STATUS.md`).
- `docs/IMPLEMENTATION_MATRIX.md` — сводная матрица: что запланировано,
  реализовано, покрыто тестами; продакшн-статус — честно «не развёрнуто».
- `docs/pages/` — документация 8 страниц: Лендинг, Авторизация, Дашборд,
  Холст, Проект, Настройки, Предпросмотр телефона с Telegram, Предпросмотр
  веба (назначение, состояния, данные, действия, планы).

### Изменено

- `docs/ROADMAP.md` — фактические отметки: модели из выделения, Drill Down,
  группы, веб-превью; уточнены ссылки на новые документы.

## [0.2.10] — 2026-09-14

### Добавлено

**Этап 2 (Промпт №2), подэтап I (часть 2) — веб-превью и развитие панели отладки**

- Вкладка «Веб» в Debug-панели: живой предпросмотр страницы, собранной из
  веб-деталей схемы (`окно браузера` с адресной строкой и перезагрузкой).
  Загрузка страницы, клик по кнопке и отправка формы запускают схему
  настоящими веб-событиями (`TriggerPayload` c `source: 'web'`) — тот же
  механизм, что у симулятора (см. `docs/WEB.md`).
- Честные пределы версии: страница собирается только из существующих
  веб-деталей (страница/кнопка/форма); элементы оформления (текст, поля,
  картинки, окна) появятся с библиотекой веб-элементов на Этапе 4.
- Вкладка «Порты» в Debug-панели: после запуска — значения входов/выходов
  каждой детали (последний прогон), статус и длительность.
- Рефакторинг стора выполнения: чистая `buildTriggerPayload()` (выбор
  источника по триггерам на схеме, сборка события) + `fireWebTrigger()`
  для превью; общий запуск вынесен в `startRun`.
- Исполнение учитывает группы холста (сериализация документа с рамками).
- 9 новых тестов (всего 100): сборка триггерного события — 5, разбор
  веб-деталей — 3, запуск схемы веб-событием — 1.

**Этап 2 завершён полностью (подэтапы A–I).**

## [0.2.9] — 2026-09-14

### Добавлено

**Этап 2 (Промпт №2), подэтап F часть 2 — Группы (визуальные рамки)**

- Опциональное поле `groups` в `CanvasDocument` (формат v1, без смены версии
  и без миграции — старые проекты остаются валидными): рамка = `id`,
  подпись и список деталей-участников.
- Рамки на холсте (`GroupFrames`): пунктирная рамка по габаритам участников,
  ярлык с названием. Клик по ярлыку выделяет участников, перетаскивание
  ярлыка двигает всю группу, двойной клик — переименование, крестик —
  убрать рамку (детали остаются).
- Пункты контекстного меню: «Сгруппировать выделенное» (2+ детали, на узле
  и на пустом холсте) и «Разгруппировать» (узел в рамке).
- Деталь принадлежит максимум одной группе; при удалении детали она
  вычищается из рамок, опустевшие рамки исчезают.
- Группы входят в снапшоты истории (undo/redo) и сохраняются в документ
  на обоих уровнях (холст проекта и холст модели), переживают смену уровня
  (Drill Down) и перезагрузку.
- При создании модели из выделенного рамки внешнего холста обрезаются:
  ушедшие в модель детали исключаются, пустые рамки удаляются.
- 13 новых тестов (всего 91): формат/сериализация групп — 3, экшены
  стора — 9, обрезка рамок при извлечении модели — 1.

## [0.2.8] — 2026-09-14

### Добавлено

**Этап 2 (Промпт №2), подэтап G — Создание моделей из Canvas**

- Пункт контекстного меню «Создать модель из выделенного» (правый клик по
  детали): выделенные детали переносятся внутрь новой модели, а на их месте
  на внешнем холсте появляется одна деталь «Вызов модели».
- Диалог «Новая модель»: имя, число выделенных деталей, автоматически
  определённый контракт (ВХОД/ВЫХОД/ОШИБКА) из граничных связей, честные
  сообщения об ошибках извлечения.
- Граничные связи увязываются через конвертеры «В объект» / «Из объекта»
  (тот же приём, что в демо-проекте): внешний источник → ключ → `вход`
  «Вызова модели»; `результат` → ключ → внешние цели; ошибки — напрямую
  через error-порт «Вызова модели».
- Чистая функция ядра `extractModel()` (`src/core/models/extract.ts`):
  контракт из типов граничных портов, внутренний холст модели с
  нормализованными позициями, пересборка внешнего холста; не зависит от
  React и пригодна для тестов.
- Работает и на главном холсте проекта, и внутри модели (модель из модели);
  после создания узел «Вызов модели» выделяется, состояние попадает в историю
  (отмена доступна).
- Текущие честные ограничения (фиксируются в диалоге и статусе): не более
  одного внешнего входа (многопортовые контракты — вместе с динамическими
  портами), триггеры внутри выделения и внешние связи-события отклоняются,
  стикеры-заметки в извлечении не участвуют.
- 11 новых тестов (всего 78): ядро `extractModel` — 8, интеграция экшена
  стора `createModelFromSelection` — 3.

## [0.2.7] — 2026-09-14

### Добавлено

**Этап 2 (Промпт №2), подэтап I (часть 1) — Предпросмотр телефона с Telegram**

- Новая вкладка «Телефон» в Debug-панели: рамка телефона, шапка бота (имя проекта, «в сети · симуляция»), пузыри сообщений — входящее сообщение пользователя и ответы бота из outbox (текст и фото-заглушка с подписью).
- Данные — из существующего симулятора (`chatEcho` + `outbox`); лента автопрокручивается.

### Примечание

- Это превью симуляции, а не реальный Telegram-транспорт (честный статус — см. `docs/agent-plan/STATUS.md`).
- Web-превью и развитие панели отладки — продолжение подэтапа I.

## [0.2.6] — 2026-09-14

### Добавлено

**Этап 2 (Промпт №2), подэтап H — Спуск внутрь (Drill Down)**

- Двойной клик по детали «Вызов модели» открывает её внутренний холст.
- Хлебные крошки `Проект › Модели › Название модели` с возвратом на уровень проекта.
- Автосохранение и мгновенное сохранение учитывают активный уровень (`withActiveCanvas`): правки записываются в холст проекта либо во внутренний холст модели (`StoredModel.canvas`), формат проекта не менялся.
- Действия стора `openModel` / `closeModel`; при смене уровня очищается история правок и сбрасывается состояние выполнения.
- Подсказка в инспекторе «Вызов модели»: как открыть модель изнутри.

### Известные ограничения (честно)

- История правок не сквозная между уровнями (очищается при переходе) — приемлемо для этапа, развитие возможно позже.
- Выполнение (симулятор) запускает основную схему; вложенные модели исполняются через неё, как и в фундаменте.

## [0.2.5] — 2026-09-14

### Добавлено

**Этап 2 (Промпт №2), подэтап F (часть 1) — Контекст и фокус**

- Контекстное меню (правый клик): дублировать, копировать, отключить связи, удалить, «следовать по связи» вперёд/назад, добавить заметку; на холсте — добавить заметку.
- Режим фокуса (🎯 в тулбаре): приглушает всё, кроме выбранной детали и её непосредственных связей; настройка сохраняется.
- Стикеры-заметки: служебный блок `note.sticky` (категория «Заметки», скрыт из палитры), редактирование текста прямо на стикере; в выполнении не участвует (портов и рантайма нет — безопасно для движка).
- Действия стора: `deleteSelection`, `disconnectNode`, `addNote`, `setNoteText`.

### Примечание

- **Группы** (визуальная группировка блоков) из подэтапа F отложены на часть 2 — требуют отдельного решения по формату; статус честно отражён в `docs/agent-plan/STATUS.md`.

## [0.2.4] — 2026-09-14

### Добавлено

**Этап 2 (Промпт №2), подэтап E — Инспектор (контекстная правая панель)**

- `InspectorPanel`: содержимое зависит от выделения.
- Инспектор соединения: блоки и порты с обеих сторон, типы портов (цветные чипы), последнее значение, прошедшее по связи, удаление соединения (с историей undo/redo).
- Инспектор холста (ничего не выбрано): название, тип проекта, версия формата, счётчики «детали · связи · модели», переменные проекта.
- Для блока «Вызов модели» в инспекторе детали — контракт выбранной модели: входы/выходы/ошибки и версия; предупреждение, если модель не выбрана.
- Выделение соединений: клик по ребру открывает инспектор соединения; выделение узлов и рёбер взаимоисключающее (`selectEdge`, `deleteEdge` в project-store).

### Примечание

- Инспектор детали (конфигурация, результаты выполнения) — из фундамента (`ConfigPanel`), сохранён без переписывания. Веб-элементы инспектируются им же — отдельная панель не создавалась, чтобы не дублировать.

## [0.2.3] — 2026-09-14

### Добавлено

**Этап 2 (Промпт №2), подэтап D — Быстрая вставка (Quick Insert)**

- Пустая схема: панель стартовых деталей — триггеры Telegram, триггеры Web, базовые значения (клик создаёт деталь в центре).
- Соединение отпущено на пустом месте: всплывающее меню только СОВМЕСТИМЫХ деталей (правила умных соединений, `quickInsertCandidates`); выбор создаёт деталь рядом и автоматически подключает к исходному порту (работает в обе стороны: от OUTPUT и от INPUT).
- Меню закрывается по клику по холсту и по Escape; не выходит за пределы холста.
- `addNode` возвращает id созданного узла (нужно для автоподключения; поведение при вставке не изменилось).

### Тесты

- 4 новых теста (итого 66): подбор кандидатов быстрой вставки (типы данных, события, ошибки, конкретный порт).

## [0.2.2] — 2026-09-14

### Добавлено

**Этап 2 (Промпт №2), подэтап C — живой UI блоков, портов и соединений (развитие фундамента)**

- Формы маркеров портов по виду: данные — круг, событие — ромб, ошибка — квадрат (классы `nzz-handle--event` / `nzz-handle--error`).
- Глифы типов портов рядом с названием типа (`Т`, `#`, `✓`, `{}`, `◇`, `[]`, `⧗`, `↗`, `✈`, `↯`, `!` и др.) с цветом типа — `PORT_TYPE_GLYPHS`/`portGlyph` в `compatibility.ts`.
- Лёгкая подсветка детали при наведении.

### Примечание

- Остальное из подэтапа C уже было в фундаменте и сохранено: цвета типов портов, умные соединения с подсветкой совместимых портов, состояния RUNNING/SUCCESS/ERROR/SKIPPED (пульс/свечение), анимация «текущих» данных по рёбрам.

### Тесты

- 2 новых теста (итого 62): глифы типов портов.

## [0.2.1] — 2026-09-14

### Добавлено

**Этап 2 (Промпт №2), подэтап B — Тулбар**

- Масштаб в тулбаре: «−», текущий процент, «+», «вписать схему в экран» (через `useReactFlow`/`useViewport`).
- Поиск по схеме: строка в тулбаре, неподходящие узлы приглушаются на Canvas, счётчик «Найдено: N»; ищет по имени экземпляра, идентификатору блока и локализованному названию (`nodeMatchesQuery`).
- Переключатель режима «Черновик / Живой» (состояние сохраняется в UI-store; семантика «Живого» режима будет дополнена вместе с публикацией — зафиксированная идея, честно отмечено в `docs/UI.md`).
- Переключатель эффектов выполнения: выключает анимацию «текущих» данных по соединениям.

### Тесты

- 7 новых тестов (итого 60): поиск по узлам схемы, режим «Черновик/Живой», эффекты, состав сохраняемого состояния UI-store.

## [0.2.0] — 2026-09-14

### Добавлено

**Этап 2 (Промпт №2), подэтап A — UI Store + Библиотека деталей v2**

- UI-store (`src/store/ui-store.ts`, Zustand + persist): вкладка библиотеки, поиск, избранное, недавние детали, свёрнутые категории. Избранное/недавние/вкладка сохраняются в LocalStorage (`nodezzle-ui-v1`), поисковая строка — состояние сессии.
- Библиотека деталей v2 (`src/features/canvas/BlockLibrary.tsx`, заменяет палитру `NodePalette`):
  - вкладки «Основные» (детали `basic`), «Все», «Избранное», «Недавние», «Мои детали», «Мои модели»;
  - поиск по названию, описанию, идентификатору и ключевым словам (`keywords`);
  - звёздочки избранного, список последних 10 вставленных деталей;
  - категории с иконками и цветами, сворачивание категорий;
  - «Мои модели»: вставка модели проекта блоком «Вызов модели» с предзаполненным `modelId`;
  - «Мои детали»: раздел-заготовка (честное пустое состояние; функциональность — после «Сохранить как деталь», Этап 2 раздел G).
- Block Definition: необязательные поля `keywords` (поиск) и `difficulty` (`basic`/`advanced`) — задел под каталог Этапа 4, обратно совместимы.
- Ключевые слова поиска и сложность проставлены всем 18 доступным блокам (`advanced`: JSON, Вход/Выход модели, В объект, Из объекта).
- `addNode` принимает переопределения конфигурации экземпляра (используется для вставки моделей).
- Чистые помощники библиотеки (`library-utils.ts`): полезная нагрузка drag & drop, поиск, список недавних.

### Изменено

- Палитра `NodePalette` заменена библиотекой `BlockLibrary` (файл `NodePalette.tsx` удалён; функциональность сохранена и расширена).
- Старые i18n-ключи `canvas.palette.*` заменены на `canvas.library.*`.

### Тесты

- 19 новых тестов (итого 53): помощники библиотеки (drag & drop, поиск, недавние) и UI-store (избранное, недавние, вкладки, категории, состав сохраняемого состояния).

## [0.1.1] — 2026-09-13

### Добавлено

**Организация работы с AI-агентами**

- Постоянный поэтапный план разработки `docs/agent-plan/` — долговременная память проекта для всех будущих AI-сессий:
  - `00-README.md` — назначение плана и обязательный порядок входа агента в работу;
  - `01-FOUNDATION.md` — Этап 1 (Промпт №1), фундамент: концепция, стек, зафиксированные подсистемы, коммит `a1b0c20`;
  - `02-CANVAS-UI.md` — Этап 2 (Промпт №2): подэтапы A–I и зафиксированные идеи (портал, рентген модели, семантический зум и др.);
  - `03-MASTER-SPEC.md` — Этап 3 (Промпт №3): полная проектная память, состав документации и страниц;
  - `04-BLOCK-LIBRARY.md` — Этап 4 (Промпт №4): 16 категорий × 10+ блоков = 160+ определений, структура определения, статусы, масштабирование до 1000+;
  - `05-DEVELOPMENT-DEPLOY.md` — Этап 5 (Промпт №5): цикл разработки, дисциплина коммитов, деплой на VPS (89.125.24.50, nodezzle.duckdns.org) с защитой чужого сайта на портах 3000/3001, безопасность секретов;
  - `STATUS.md` — точное место остановки разработки (заполнено по фактическому Git);
  - `RULES.md` — 16 обязательных правил.
- В `AGENTS.md` добавлено обязательное правило: перед любой разработкой читать `docs/agent-plan/00-README.md`, `STATUS.md`, `RULES.md`, затем документ текущего этапа.

## [0.1.0] — 2026-09-13

### Добавлено

**Фундамент проекта**

- Инициализация структуры NODEZZLE: ядро, блоки, экраны, документация (12 документов на русском).
- Технологический стек: Vite + React 19 + TypeScript, @xyflow/react (React Flow 12), Zustand, i18next (ru-RU), Tailwind CSS 4, Zod 4, Vitest. Обоснование — [docs/DECISIONS.md](docs/DECISIONS.md).
- `.gitignore` и безопасный `.env.example` (только названия переменных и фиктивные значения).

**Ядро (src/core)**

- Block Definition + BlockRegistry: расширяемая регистрация деталей; ядро не привязано к конкретным платформам.
- Система типов портов: 16 базовых типов (Text, Number, Boolean, JSON, Object, Array, File, Image, User, Date, URL, Secret, TelegramMessage, Any, Event, Error) + TypeRegistry для пользовательских типов.
- Умные соединения: правила совместимости INPUT/OUTPUT (data ↔ data по типу, `any` со всем, event ↔ event, error ↔ error); несовместимые соединения запрещены.
- Runtime (`executeCanvas`): живое выполнение схем — состояния IDLE/WAITING/RUNNING/SUCCESS/ERROR/STOPPED, pull-выполнение «холодных» источников, защита от циклов, отмена (Stop), телеметрия (входы/выходы/время/ошибки), outbox для Telegram-отправок (симуляция).
- Модели: контракт (INPUT/OUTPUT/ERROR), рекурсивное выполнение «модель внутри модели» (`executeModel`), блоки «Вход модели»/«Выход модели»/«Вызов модели».
- Формат проекта v1 (Zod-схема): canvas, узлы, соединения, модели, контракты, переменные, meta; адаптер ProjectStorage (LocalStorage, flush при закрытии вкладки).

**Блоки (24 зарегистрированы, 18 в палитре)**

- Ядро: Текст, Число, JSON, Вход модели, Выход модели.
- Данные (конвертеры): Текст→Число, Число→Текст, Текст→Логическое, Текст→JSON, JSON→Текст, В объект, Из объекта.
- Логика: Условие (8 операторов, значения веток), Переключатель (3 случая + Иначе).
- Поток: Задержка (с отменой).
- Telegram: Получено сообщение (триггер), Команда (триггер), Отправить сообщение, Отправить фото (симуляция).
- Web: Страница, Кнопка, Форма (триггеры событий, симуляция).
- Модели: Вызов модели.
- Отладка: Лог.
- Дата и время: Текущее время.
- Зарезервированы (available: false): Переменная, Общее хранилище, Обработчик ошибок, Отправить/Получить событие, HTTP-запрос, AI: запрос.

**Интерфейс (ru-RU)**

- Главная: Hero с анимацией соединяющихся деталей, Playground (интерактивная демонстрация с демо-чатом), возможности, Premium Dark UI (glassmorphism, aurora, glow).
- Dashboard: проекты, создание (Telegram-бот / Web / HTML / Telegram + Web / Пустой), загрузка демо-примера, удаление.
- Canvas: библиотека деталей (категории, поиск, drag & drop), кастомные узлы из Block Definition, умные соединения с подсветкой совместимых портов, undo/redo (Ctrl+Z/Ctrl+Shift+Z), копировать/вставить/дублировать (Ctrl+C/V/D), Delete, minimap, autosave (debounce 900 мс + beforeunload), инспектор детали (конфигурация + результат последнего выполнения).
- Debug-панель: Симулятор (Telegram/Web payload), Чат Telegram (outbox), Журнал выполнения (LOG), История (execution history); частицы/анимация по соединениям во время выполнения.

**Демо**

- Демонстрационный проект «Пример: Telegram-приветствие»: `Получено сообщение → В объект → Вызов модели «Приветствие» → Из объекта → Отправить сообщение`; модель с контрактом (text → reply).

**Тесты**

- 34 unit-теста: система совместимости, реестр, формат проекта, хранилище, runtime (демо-схема, ветвление, конвертеры, ошибки, команды, логи, циклы, задержка, отмена, модель внутри модели).

### Документация

- README.md, AGENTS.md, CHANGELOG.md.
- docs/: CONCEPT, ARCHITECTURE, BLOCKS, MODELS, TELEGRAM, WEB, UI, PROJECT_FORMAT, ROADMAP, IDEAS, DECISIONS.
