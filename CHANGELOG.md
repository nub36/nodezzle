# CHANGELOG — NODEZZLE

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/).
Версия = `formatVersion` формата проекта + стабильность ядра.

## [0.2.0] — 2026-09-14

### Добавлено

**Этап 2 (Промпт №2), подэтап A — UI Store + Библиотека деталей v2**

- UI-store (`src/store/ui-store.ts`, Zustand + persist): вкладка библиотеки, поиск, избранное, недавние детали, свёрнутые категории. Избранное/недавние/вкладка сохраняются в LocalStorage (`nodezzle-ui-v1`), поисковая строка — состояние сессии.
- Библиотека деталей v2 (`src/features/canvas/BlockLibrary.tsx`, заменяет палитру `NodePalette`):
  - вкладки «Основные» (детали `basic`), «Все», «Избранное», «Недавние», «Мои детали», «Мои модели»;
  - поиск по названию, описанию, идентификатору и ключевым словам (`keywords`);
  - звёздочки избранного, список последних 10 вставленных деталей;
  - категории с иконками и цветами, сворачивание категорий;
  - «Мои модели»: вставка модели проекта блоком «Вызов модели» с предзаполненным `modelId`;
  - «Мои детали»: раздел-заготовка (честное пустое состояние; функциональность — после «Сохранить как деталь», Этап 2 раздел G).
- Block Definition: необязательные поля `keywords` (поиск) и `difficulty` (`basic`/`advanced`) — задел под каталог Этапа 4, обратно совместимы.
- Ключевые слова поиска и сложность проставлены всем 18 доступным блокам (`advanced`: JSON, Вход/Выход модели, В объект, Из объекта).
- `addNode` принимает переопределения конфигурации экземпляра (используется для вставки моделей).
- Чистые помощники библиотеки (`library-utils.ts`): полезная нагрузка drag & drop, поиск, список недавних.

### Изменено

- Палитра `NodePalette` заменена библиотекой `BlockLibrary` (файл `NodePalette.tsx` удалён; функциональность сохранена и расширена).
- Старые i18n-ключи `canvas.palette.*` заменены на `canvas.library.*`.

### Тесты

- 19 новых тестов (итого 53): помощники библиотеки (drag & drop, поиск, недавние) и UI-store (избранное, недавние, вкладки, категории, состав сохраняемого состояния).

## [0.1.1] — 2026-09-13

### Добавлено

**Организация работы с AI-агентами**

- Постоянный поэтапный план разработки `docs/agent-plan/` — долговременная память проекта для всех будущих AI-сессий:
  - `00-README.md` — назначение плана и обязательный порядок входа агента в работу;
  - `01-FOUNDATION.md` — Этап 1 (Промпт №1), фундамент: концепция, стек, зафиксированные подсистемы, коммит `a1b0c20`;
  - `02-CANVAS-UI.md` — Этап 2 (Промпт №2): подэтапы A–I и зафиксированные идеи (портал, рентген модели, семантический зум и др.);
  - `03-MASTER-SPEC.md` — Этап 3 (Промпт №3): полная проектная память, состав документации и страниц;
  - `04-BLOCK-LIBRARY.md` — Этап 4 (Промпт №4): 16 категорий × 10+ блоков = 160+ определений, структура определения, статусы, масштабирование до 1000+;
  - `05-DEVELOPMENT-DEPLOY.md` — Этап 5 (Промпт №5): цикл разработки, дисциплина коммитов, деплой на VPS (89.125.24.50, nodezzle.duckdns.org) с защитой чужого сайта на портах 3000/3001, безопасность секретов;
  - `STATUS.md` — точное место остановки разработки (заполнено по фактическому Git);
  - `RULES.md` — 16 обязательных правил.
- В `AGENTS.md` добавлено обязательное правило: перед любой разработкой читать `docs/agent-plan/00-README.md`, `STATUS.md`, `RULES.md`, затем документ текущего этапа.

## [0.1.0] — 2026-09-13

### Добавлено

**Фундамент проекта**

- Инициализация структуры NODEZZLE: ядро, блоки, экраны, документация (12 документов на русском).
- Технологический стек: Vite + React 19 + TypeScript, @xyflow/react (React Flow 12), Zustand, i18next (ru-RU), Tailwind CSS 4, Zod 4, Vitest. Обоснование — [docs/DECISIONS.md](docs/DECISIONS.md).
- `.gitignore` и безопасный `.env.example` (только названия переменных и фиктивные значения).

**Ядро (src/core)**

- Block Definition + BlockRegistry: расширяемая регистрация деталей; ядро не привязано к конкретным платформам.
- Система типов портов: 16 базовых типов (Text, Number, Boolean, JSON, Object, Array, File, Image, User, Date, URL, Secret, TelegramMessage, Any, Event, Error) + TypeRegistry для пользовательских типов.
- Умные соединения: правила совместимости INPUT/OUTPUT (data ↔ data по типу, `any` со всем, event ↔ event, error ↔ error); несовместимые соединения запрещены.
- Runtime (`executeCanvas`): живое выполнение схем — состояния IDLE/WAITING/RUNNING/SUCCESS/ERROR/STOPPED, pull-выполнение «холодных» источников, защита от циклов, отмена (Stop), телеметрия (входы/выходы/время/ошибки), outbox для Telegram-отправок (симуляция).
- Модели: контракт (INPUT/OUTPUT/ERROR), рекурсивное выполнение «модель внутри модели» (`executeModel`), блоки «Вход модели»/«Выход модели»/«Вызов модели».
- Формат проекта v1 (Zod-схема): canvas, узлы, соединения, модели, контракты, переменные, meta; адаптер ProjectStorage (LocalStorage, flush при закрытии вкладки).

**Блоки (24 зарегистрированы, 18 в палитре)**

- Ядро: Текст, Число, JSON, Вход модели, Выход модели.
- Данные (конвертеры): Текст→Число, Число→Текст, Текст→Логическое, Текст→JSON, JSON→Текст, В объект, Из объекта.
- Логика: Условие (8 операторов, значения веток), Переключатель (3 случая + Иначе).
- Поток: Задержка (с отменой).
- Telegram: Получено сообщение (триггер), Команда (триггер), Отправить сообщение, Отправить фото (симуляция).
- Web: Страница, Кнопка, Форма (триггеры событий, симуляция).
- Модели: Вызов модели.
- Отладка: Лог.
- Дата и время: Текущее время.
- Зарезервированы (available: false): Переменная, Общее хранилище, Обработчик ошибок, Отправить/Получить событие, HTTP-запрос, AI: запрос.

**Интерфейс (ru-RU)**

- Главная: Hero с анимацией соединяющихся деталей, Playground (интерактивная демонстрация с демо-чатом), возможности, Premium Dark UI (glassmorphism, aurora, glow).
- Dashboard: проекты, создание (Telegram-бот / Web / HTML / Telegram + Web / Пустой), загрузка демо-примера, удаление.
- Canvas: библиотека деталей (категории, поиск, drag & drop), кастомные узлы из Block Definition, умные соединения с подсветкой совместимых портов, undo/redo (Ctrl+Z/Ctrl+Shift+Z), копировать/вставить/дублировать (Ctrl+C/V/D), Delete, minimap, autosave (debounce 900 мс + beforeunload), инспектор детали (конфигурация + результат последнего выполнения).
- Debug-панель: Симулятор (Telegram/Web payload), Чат Telegram (outbox), Журнал выполнения (LOG), История (execution history); частицы/анимация по соединениям во время выполнения.

**Демо**

- Демонстрационный проект «Пример: Telegram-приветствие»: `Получено сообщение → В объект → Вызов модели «Приветствие» → Из объекта → Отправить сообщение`; модель с контрактом (text → reply).

**Тесты**

- 34 unit-теста: система совместимости, реестр, формат проекта, хранилище, runtime (демо-схема, ветвление, конвертеры, ошибки, команды, логи, циклы, задержка, отмена, модель внутри модели).

### Документация

- README.md, AGENTS.md, CHANGELOG.md.
- docs/: CONCEPT, ARCHITECTURE, BLOCKS, MODELS, TELEGRAM, WEB, UI, PROJECT_FORMAT, ROADMAP, IDEAS, DECISIONS.
